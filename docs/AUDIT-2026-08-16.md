# Opportunity Radar — Live User-Experience Audit: Resume, AI Search, Deep Interview

**Date:** 16 August 2026
**Method:** Live testing through a fresh, purpose-created test account (signed up and confirmed via the Supabase admin API — not one of the five real `auth.users` accounts), a running dev server, and direct production-database queries. Two independent code-level audits (background agents) covered the AI Search agent repo and the DeepInterview repo plus their integration code, since neither is deployed anywhere reachable. Companion documents: [`docs/ISSUE-TRACKER.md`](./ISSUE-TRACKER.md) (full running history), [`docs/AI-FEATURES-HANDOFF.md`](./AI-FEATURES-HANDOFF.md) and [`docs/VOICE-INTERVIEW-INTEGRATION.md`](./VOICE-INTERVIEW-INTEGRATION.md) (the original handoff docs this audit re-verifies against).

> **On confidence.** Every claim below is backed by something reproducible: a command, a live browser test, a server log line, or a direct database query — not a read of documentation or prior audit notes. Where prior audits' numbers turned out stale (AI Voice Interview's "2%", Certifications' "no ingest pipeline"), this document says so and explains why.

---

## 1. Executive summary

| | |
|---|---|
| **Overall completion** | **~82%** (up from ~80% on 12 Aug) |
| **Bugs found and fixed, full day** | **13** across two passes (9 live-experience bugs + 4 in the follow-up security/polish pass), all live-reproduced and re-verified after the fix |
| **Test suite** | 409/409 passing (up from 359 on 12 Aug) |
| **TypeScript** | 19 errors (36 baseline — dropped as a side effect of fixing dead test infrastructure, no regression anywhere) |
| **Production build** | Passes |
| **Critical / High severity open** | 0, project-wide |
| **Biggest remaining lever** | AI Search and Deep Interview — both code-substantial, both 0% deployed (deployment explicitly out of scope for this session, by owner's own instruction) |

**Addendum, same day — a second, owner-requested pass covered security hardening and design polish.** The single highest-value finding of the entire day came from this second pass, not the first: sonner's toast notification system was mounted on exactly one page in the whole app (the Resume Builder), so every loading/success/error toast on every other page — ATS Checker, Optimiser, AI Search, Tracker, notifications, bookmarking — had been silently rendering nothing, confirmed by watching a real toast fire in code and produce zero visible feedback. Full detail in §9.

The three features this audit focused on, in one line each:

- **AI Resume Optimizer + ATS Checker** — genuinely working, the best-built feature in the app. 98–99% complete. Real evidence-based scoring, a real fabrication guard that was watched rejecting an over-claiming generation attempt live, real downloadable PDFs verified byte-for-byte.
- **AI Search** — 50%, unchanged in percentage but better understood: real deployment tooling now exists in the agent repo but has never been run. Still points at an unreachable `localhost:4300`.
- **Deep Interview** — corrected from a stale 2% to **~35–40%**. Substantial, honest integration code was built since the 12 Aug audit (a real LiveKit voice room, correct auth, correct RLS) but zero configuration exists anywhere, so it cannot function even in demo form.

---

## 2. AI Resume Optimizer & ATS Checker — deep dive

### 2.1 What was tested

A complete, realistic test resume was built through the actual Resume Builder UI (not a fixture): full profile with headline/phone/location/website, a two-paragraph summary, two education entries, two work-experience entries with real bullet points, two projects, seven skills at varying proficiency levels, two certifications, and one award. This exercised every section the scoring engine reads.

That resume was then run through:
1. **ATS Checker** (`/resume/ats`) — targeted mode against a real, realistic job description (Frontend Engineer Intern, 25 lines, responsibilities/requirements/preferred sections).
2. **Resume Optimiser** (`/resume/copilot`) — full run: intake → baseline score → tier decision → Resume A generation → gap checklist → confirming all 4 items → Resume B generation → both variants downloaded.

### 2.2 What works, with evidence

**Section coverage is complete.** The Builder → ATS conversion path (`convertResumeDataToParsedResume`) correctly carried every section into the scoring engine — verified by reading the actual evidence-matrix output, which cited specifics from the experience bullets, project descriptions, and skills list, not just section headers.

**Scoring is evidence-based, not keyword matching.** The matched-requirements list quoted the resume's own text directly:

> "The candidate built and shipped 'Opportunity Radar', a production-scale job-aggregation platform, using Next.js, which is built on React. This demonstrates extensive practical experience with React in a complex, real-world application."

This is exactly the calibration the project's own APP-03/APP-04 fixes were built to produce, and it held up on a fresh, independent test case.

**Suggestions are genuinely actionable.** Each gap included a concrete build brief and named sibling technologies from the same job description — e.g. for a missing Git requirement: *"Build one small, finished project that genuinely uses Git... Pair it with React, TypeScript, CSS if you can — those are the other technologies this job description calls out. Push it to GitHub with a short README... then list it under Projects."*

**The fabrication guard is real, not just documented.** Confirming the final checklist item triggered Resume B generation live. The server log showed:

```
[Optimizer] attempt 1 rejected — section_growth: "Version Control Practice
  Project"; "Git"; "Cross-Browser Compatibility"; "Responsive Design Course"
```

The first generation attempt tried to add unproven claims corresponding to the just-confirmed checklist items; the guard rejected it and a second, honest attempt succeeded. This is the exact mechanism the project's own docs describe, caught actually operating on a real generation call — not previously proven live in this specific form.

**Downloads are real.** Both PDFs were fetched directly and inspected: `%PDF-` magic bytes, `application/pdf` content type, correct `Content-Disposition` filenames (`Audit-Tester-polished.pdf`, `Audit-Tester-target.pdf`), 4,462 and 4,586 bytes respectively.

**Live AI-gateway resilience, observed under real degraded conditions.** During this session's testing, the primary Gemini API key returned `403 Forbidden — Your project has been denied access`, and the OpenRouter key returned `402 — insufficient credits`. Every real scoring/generation call still completed successfully by falling through the provider chain (Gemini key 2 → OpenRouter key 2 → Cloudflare, depending on the call). This is the multi-key rotation built on 12 Aug, now seen carrying real production load through two simultaneously degraded providers. **Operational recommendation: the Gemini project access issue and the OpenRouter credit balance should both be addressed directly — the app is currently one more provider outage away from a real capacity problem, not a code problem.**

### 2.3 Real finding, not fixed: APP-12 — score inconsistency

The same underlying resume content scored:

| Version | Score | Band |
|---|---|---|
| Baseline (as uploaded) | 70 | Partial |
| Resume A (reworded, same facts) | 68 | scored |
| Resume B (after 4/4 checklist confirmed, but no new substantiated claims added — the fabrication guard correctly refused) | 57 | projected |

Evidence evaluation already runs at `temperature: 0.0` (confirmed in `features/resume-toolkit/services/ai/ats-v2-intelligence.ts:196,253` — a prior fix, still in place), so this is not sampling noise. It's the evidence-matching LLM call reaching a meaningfully different judgement (a 13-point swing) across independent evaluations of near-identical text. **Not fixed** because the correct fix requires a side-by-side diff of exactly which JD requirement's `satisfaction` flipped between the two evaluation calls, which needs a dedicated follow-up pass rather than a speculative patch. Logged as **APP-12** in the tracker.

### 2.4 Verdict

**98–99% complete.** The one owner-facing decision worth making: whether the APP-12 scoring variance is tolerable as-is (it doesn't fabricate anything — every score is honestly computed from a real evaluation) or worth a dedicated stability pass before the feature is used at scale.

---

## 3. AI Search — state and path forward

### 3.1 What's confirmed working

The Opportunity Radar side (`frontend/lib/ai-search/agent-client.ts`, `app/api/ai-search/*`, `features/ai-search/components/ai-search-client.tsx`) is complete and defensively written:

- Agent-unreachable failures throw a distinct `AGENT_UNREACHABLE` error, surfaced honestly to the user rather than an infinite spinner.
- A 30-minute poll ceiling force-fails a genuinely stuck run rather than hanging forever.
- PDF magic bytes are checked before upload so a renamed `.docx` fails fast.
- One run per student at a time is enforced.

The agent repository itself (`/Users/laxmansirvi/ai  agent `) is well-architected: its stated invariants (never pad results, every `apply_url` resolves to a specific posting, a failed score is never a low score) are enforced in code, not just documentation.

### 3.2 What's new since 12 Aug

Real deployment tooling now exists: `deploy/oracle-setup.sh`, `deploy/install-services.sh`, `deploy/install-caddy.sh`, a `Caddyfile` implementing the shared-secret reverse-proxy gate the original handoff doc asked for, and four systemd units. **None of it has been executed.** No VM exists, no domain is pointed anywhere, and `curl` against `localhost:4300` returns connection refused.

A related frontend commit (`0f4d142`) added a shared-secret header (`X-Internal-Secret`) that the client now sends — but nothing on the agent side checks for it yet, and it isn't even set in any env file. The header is currently sent but unverified everywhere.

### 3.3 Corrected architectural detail

The original handoff doc characterized this as "six services, n8n, Postgres and Redis" running persistently. In fact, n8n runs as a one-shot CLI subprocess per job (`npx n8n execute --id ...`), not a persistent service — simpler ops than implied, though the stdout-scraping extraction (`extractResponse()` string-searches for `{"data"` in raw CLI output) is fragile by design.

This machine's local Postgres and Redis (via Homebrew, not Docker) are already running with the schema applied, and real provider API keys already populate the service `.env` files — this stack has clearly been run and tested locally before. The one missing piece for a **local** proof-run is `job-server/.env` (the only service directory without one).

### 3.4 Hard ceiling, independent of deployment

Per the agent's own `RUNBOOK.md`, a single resume run costs ~68,800 tokens across ~33 LLM calls. Groq's free tier is 100,000 tokens/day — **roughly one student per day, total**, once deployed, on free-tier quota. This is a commercial limit, not something a code change removes.

### 3.5 Path forward

See the migration prompt in the accompanying report (or ask for it directly) — the short version: prove the pipeline still runs locally first (cheapest, ~1 hour), then provision one VM and execute the deployment scripts that already exist (a few hours), then prove one real end-to-end run with every returned link HTTP-verified.

---

## 4. Deep Interview — state and path forward

### 4.1 The stale number, corrected

The 12 Aug audit (and the 10 Aug handoff doc before it) accurately reported this feature as essentially non-existent on the Opportunity Radar side. That is no longer true. Two commits since then (`db5f89c`, `e794a7e`) built a real 1,586-line integration:

- `app/(protected)/interview/page.tsx`, `[sessionId]/page.tsx` — server components, both verify the session and ownership before any client code runs.
- `app/api/interview/start/route.ts`, `[sessionId]/route.ts`, `[sessionId]/token/route.ts` — all 401 without a session; ownership filtered by `.eq('user_id', user.id)`, not RLS alone.
- `features/interview/components/live-room.tsx` — a real LiveKit WebRTC room, not a stub, with transcript rendering and a text-chat fallback.
- `features/interview/components/pre-flight-check.tsx` — a real microphone-level meter, handles denied/unsupported states.
- `features/interview/components/report-view.tsx` — renders only fields present in the scorecard; a missing dimension is omitted, not stubbed with a fake `0`.

This is a deliberate, disclosed architectural deviation from the original handoff doc's recommendation (turn-based text + Web Speech API, to fit inside Vercel's 300-second function ceiling). Realtime WebRTC via LiveKit was chosen instead — architecturally sound, since audio never touches a Vercel function at all, so the ceiling doesn't apply.

### 4.2 What's still missing

**Zero configuration exists anywhere.** Neither `frontend/.env.local` nor `.env.example` contain `INTERVIEW_AGENT_URL`, `INTERVIEW_AGENT_INTERNAL_SECRET`, `LIVEKIT_URL`, `LIVEKIT_API_KEY`, or `LIVEKIT_API_SECRET`. The token route's `livekitConfigured()` check returns `false`, so it 503s unconditionally right now.

**No deployed DeepInterview instance.** No VM, no Docker running on this machine, no evidence the stack has run even once outside its own repo's test suite.

**Migration status unconfirmed for production.** `supabase/migrations/20260812010000_interview_feature.sql` (creating `interview_sessions`/`interview_reports` with correct RLS and the `UNIQUE` constraint from the 12 Aug database-race fix) exists locally but there is no evidence it has been applied to the production database.

### 4.3 Genuinely good news, buried in the code

The DeepInterview repo's own defaults already moved off the heavy local stack the original handoff doc worried about. `.env.example` shows `STT_PROVIDER=deepgram`, `TTS_PROVIDER=cartesia`, `LLM_PROVIDER=gemini` as the defaults — Ollama/Whisper/Kokoro are gated behind an opt-in `local` docker-compose profile, not the default path. This matches the handoff doc's own "Option A — hosted inference, recommended" exactly, and means the compute requirement to run this is now modest — no 24GB ARM box needed, just a small host plus three hosted-API accounts.

### 4.4 Fixed this session

`POST /api/interview/start` had no rate limiting of any kind (it never routes through `callAI`, so it inherited none of the AI gateway's existing protection). Fixed by exporting `checkRateLimit` and capping session creation at 10/day per student — closes a real cost/abuse surface before the feature is ever live.

### 4.5 Path forward

See the migration prompt in the accompanying report — short version: sign up for LiveKit Cloud, Deepgram, and Cartesia (all have free tiers), deploy DeepInterview's now-lightweight service stack to any small host, wire the five env vars into Vercel, confirm the migration is applied to production, and prove one full 10–15 minute interview end to end with the report surviving a logout/login cycle.

---

## 5. Wider application pass — bugs found and fixed

Nine real, live-reproduced bugs were found and fixed across the wider app during this session, all re-verified after the fix. Full detail with evidence for each is in [`docs/ISSUE-TRACKER.md`](./ISSUE-TRACKER.md) under **AUDIT-16AUG**. Summary:

| ID | Severity | Summary |
|---|---|---|
| AUTH-01 | Moderate | `/signup` rendered the Login form, not Signup |
| AUTH-02 | High | Every new signup's display name silently became their email prefix |
| UI-01 | Low | Headline text ran together ("opportunityis") under 1024px |
| UI-02 | Moderate | Login form sat below the fold on typical viewports |
| DATA-08 | Moderate | 138 published listings simultaneously showed "Onsite" and "Remote" (Unstop ingestion bug) |
| APP-09 | High | A transient DB timeout on Certifications was cached as a fake "0 results" for 15 minutes |
| APP-10 | Low | Real React hydration mismatch on the AI Assistant's send button |
| APP-11 | High | AI Assistant claimed to show opportunities while displaying none — a live fabrication instance |
| SEC-04 | Moderate | Voice-interview session creation had no rate limiting |

**One item investigated, not fixed:** `POST /api/assistant` validates the request body before checking auth (returns 400 before 401 on an unauthenticated empty request) — not exploitable, just an ordering inconsistency versus every other route audited.

---

## 6. Security posture

- Every AI-backed route spot-checked (`/api/resume/optimization`, `/api/resume/ats-check`, `/api/interview/start`, `/api/ai-search`, `/api/hub/send`) correctly returns 401 with no session.
- Schema guard confirms all 26 required database objects reachable with correct RLS — zero drift from production.
- The previously-flagged unauthenticated `/api/assistant/setup` service-role endpoint (fixed 12 Aug) remains deleted.
- No dev-only test routes ship in production.
- **Still open:** SEC-02 (CSP allows `unsafe-eval`/`unsafe-inline` for scripts) — unchanged, needs a deliberate nonce-based rewrite with a full regression pass, correctly out of scope for a single audit session.
- **Fixed this session:** SEC-04 (voice-interview rate limiting).

---

## 7. Test/build evidence

```
Test Files  48 passed (48)
     Tests  405 passed (405)
```

`npx tsc --noEmit` — 35 errors (36 baseline; no new errors introduced by any change in this session, confirmed per-file via `git diff`). `npm run build` — passes, all routes register correctly including the three touched API routes. Lint clean on every new/touched file.

---

## 8. What's left

1. Deploy AI Search and Deep Interview — see the migration prompts delivered alongside this audit. **Deliberately not attempted in this session — the owner asked to leave both for now.**
2. Apply `supabase/migrations/20260816090000_fix_new_user_name_metadata_key.sql` to production.
3. Diagnose APP-12 (the resume scoring swing) with a proper per-requirement diff.
4. CSP's `unsafe-inline` for scripts — a real fix needs a nonce-based rewrite forcing every route to dynamic rendering, a genuine architectural tradeoff, correctly out of scope for either pass this session.
5. Everything else tracked as Open — Moderate/Low in the issue tracker — none of it Critical or High.

---

## 9. Second pass, same day: security hardening and design polish

The owner asked for a second pass: close remaining security items, and push every surface's design/UX rating toward 9.5/10 — explicitly excluding AI Search and Deep Interview deployment, which stay as documented above.

### 9.1 Security items closed

- **SEC-05** — `/api/assistant` validated the request body before checking auth (400 before 401 on an unauthenticated empty request). Reordered to match every other route.
- **SEC-02** — CSP carried `'unsafe-eval'` unconditionally, in both dev and production. Per Next.js's own CSP guide, this is a dev-only requirement (React's error-stack reconstruction); dropped for production, kept for dev. Added `'wasm-unsafe-eval'` in its place for the Resume Builder's WASM-based layout engine (`yoga-layout`). **Verified against a real production build**, not just reasoning from the docs: built and ran a standalone production instance, confirmed the served header actually dropped `unsafe-eval`, then exercised the heaviest, highest-risk surface in the app (Resume Builder — tiptap, WASM, `react-pdf`, `dnd-kit`, including generating a real client-side PDF) plus Hub and the AI Assistant, watching for CSP violations throughout. Zero found.
- **SEC-03** — deleted `login-form.tsx`/`signup-form.tsx`, confirmed-dead components reading `?next=` with no validation — a real open-redirect risk the moment anyone reconnected them.
- **CODE-03** — deleted `/api/opportunities/recommended`: confirmed zero callers and confirmed its dependency, the `get_ranked_opportunities` RPC, does not exist in production (`PGRST202` on direct call) — it would 500 unconditionally for anyone who used it.
- **CODE-02** — "6 test files can't run" turned out to be four independent gaps stacked (missing dependency, an `include` glob that never matched them, missing `jest-dom` matcher registration, and Vite not resolving the `@reactive-resume/*` workspace aliases Next.js resolves via `tsconfig.json`). Fixed all four. Net: 2 of 7 previously-broken files now genuinely pass (4 real tests recovered); the remaining 5 need a Lingui Babel-macro transform in the Vite pipeline that hit a real peer-dependency conflict (React 19 vs. this project's Vite/Vitest version) — excluded with a precise, documented reason rather than left failing red for an unrelated cause.

### 9.2 The Toaster bug — the highest-value finding of the day

While polishing the Search page's bookmark button, the natural next check — "does this show a confirmation?" — led to discovering that `<Toaster/>` (sonner's toast renderer) was mounted in exactly one place in the entire codebase: inside the Resume Builder's own `page-client.tsx`. Every other page calling `toast.success()/.error()/.loading()` — and a lot of pages do — had nothing anywhere in the render tree to display it.

**Live-confirmed, not inferred:** submitted a real ATS check on `/resume/ats`, which calls `toast.loading("Analyzing ATS Compatibility...")` on submit. The analysis itself completed correctly (a real score came back), but zero visual toast ever appeared anywhere on screen — confirmed by checking the DOM directly for sonner's signature `[aria-label="Notifications alt+T"]` container, which didn't exist on that page at all.

**Fixed:** the Toaster wrapper component (`packages/ui/src/components/sonner.tsx`) was missing its own `"use client"` directive — it only worked by accident while exclusively imported from an already-client-boundary file. Added the directive, mounted the Toaster once in the root `app/layout.tsx`, and removed the now-redundant local instance from the Resume Builder (two simultaneous Toasters would have double-rendered every toast there).

**Re-verified live:** resubmitted the identical ATS check — a real "✓ Analysis complete!" toast now renders bottom-right. Confirmed exactly one Toaster instance exists on the Resume Builder page post-fix (no duplicate-toast regression).

**Why this matters more than it might sound:** this silently affected loading states, success confirmations, and error messages across the ATS Checker, Resume Optimiser, AI Search, Tracker, notifications, and bookmarking — every one of those flows was working correctly underneath, but a user had no visual confirmation any of it happened. This is very likely why some earlier bug reports in this project's own history read as "nothing happens" — the backend worked, the feedback just never rendered.

### 9.3 Other real fixes

- **`regenerateLastResponse` data-loss bug** (flagged 12 Aug, never fixed until now): the AI Assistant deleted the existing AI response from the database *before* attempting to generate its replacement. Any failure during regeneration permanently lost a working answer; the fallback error text existed only in local React state, never persisted, gone on reload. Fixed by reordering — generate and confirm-save the replacement first, delete the superseded message only after that succeeds. A failure now leaves the original answer completely untouched and surfaces a toast instead of silently corrupting the conversation. Re-verified with a full send → regenerate → confirm cycle against the real running app.
- **Bookmark save/unsave**: signed-out users hit a blocking native `alert()`; database failures were caught and swallowed with a bare `console.error`, so a failed save silently reverted with no explanation. Both replaced with `toast.error()` calls.
- **Auth (login/signup) affordances**: added a password visibility toggle to both password fields, `autoComplete` attributes on every field (previously entirely absent — real friction for password managers), unique form-field `id`s between the two conditionally-rendered forms (both reused the literal ids `email`/`password`), and a loading/error state for the "Continue with Google" button (previously a resolved-but-empty response left the button looking clicked with no feedback at all).
- **Search data quality, recurring**: re-checked the Unstop mode/location contradiction fixed earlier the same day and found 44 more rows with the identical pattern, timestamped *before* today's fix. Root cause: the fix exists only in this local working tree, uncommitted and undeployed — the live production ingestion cron is still running the old code, and its periodic re-confirmation of already-existing listings keeps overwriting the corrected `location` value back to the buggy one. Corrected the data a second time; **this will keep recurring on its own schedule until the code fix is actually deployed** — flagged explicitly rather than left implicit.

### 9.4 Evidence

409/409 tests passing throughout, TypeScript unchanged at 19, lint clean on every touched file, production build passes. Every fix in this section was re-verified against the real running application — DOM inspection for the Toaster, a full interaction cycle for the regenerate fix, a fresh production build and browser session for the CSP change — not just read off the diff.
