# Issue Tracker — Opportunity Radar

Living record of every issue found in the 10 Aug 2026 audit and what has been done about it.
Source of findings: [`AUDIT-2026-08-10.md`](./AUDIT-2026-08-10.md) · [`AUDIT-2026-08-16.md`](./AUDIT-2026-08-16.md) (Resume/AI Search/Deep Interview deep dive) · AI features: [`AI-FEATURES-HANDOFF.md`](./AI-FEATURES-HANDOFF.md)

**Rules for this file**
- Nothing moves to ✅ **Fixed** without evidence — a command, a query result, or a test. Write the evidence down.
- "Should work now" is not evidence. If it was not verified, it stays 🟡 **In progress**.
- When a fix is deployed, record the date. Fixed-locally and fixed-in-production are different states.

**Status key:** ✅ Fixed · 🟡 In progress · ⬜ Open · ⏸️ Deferred (deliberate) · ❌ Won't fix

---

## Score card

This table only ever counted DB-01–05, APP-01, OPS-02, SEC-01 and DATA-01–03
by severity — it was never updated once APP-02 through APP-08 started
shipping (each of those is itself several real bugs, not one), so it has
been understating "Fixed" since 10 Aug. Rather than retroactively guess a
severity for entries that never had one, here is a plain count of every
top-level tracked item instead — a more honest number than a stale Sev split.

| | Count |
|---|---|
| **Fixed** | **37** — DB-01–05, OPS-02, APP-01–09, SEC-01–05, DATA-01–03, DATA-08, UI-01–03, AUTH-01–02, CODE-03, CLEAN-05, plus the Toaster + regenerate + bookmark + auth-affordance fixes in AUDIT-16AUG-POLISH (untracked IDs — see that section) |
| Open | 9 — DATA-04 closed and DATA-05's data corrected 17 Aug (its cause still open) |
| Deferred | 2 — LEGAL-01, OPS-01 |
| **Total tracked** | **48+** |

**No critical or high-severity issue remains open.** Everything still open is
Moderate or Low.

Last updated: **17 Aug 2026**

---

## ✅ Fixed

### DB-01 · Seven tables missing in production — **CRITICAL**
**Found:** `certifications`, `ai_search_jobs`, `resume_optimizations`, `resume_ats_reports` did not exist. Blocked AI Search, Certifications and Resume Optimisation entirely.
**Cause:** `20260626000000_resume_toolkit_v2.sql` applied only partially — `resumes` landed, the other three objects did not. Later migrations that `ALTER` those tables therefore could not run either.
**Fixed:** 10 Aug 2026. Applied in dependency order via `supabase db query --linked`:
1. `20260809120000_certifications.sql`
2. `20260809180000_ai_search_jobs.sql`
3. `20260810120000_repair_resume_toolkit_objects.sql` *(new — see DB-05)*
4. `20260810090000_resume_optimization.sql`

**Evidence** — `pg_class` after the change:
```
table                    rls    policies  service_role  authenticated
ai_search_jobs          true       3      true          true
certifications          true       1      true          true
resume_ats_reports      true       1      true          true
resume_optimizations    true       4      true          true
```
`resume_optimizations` has all 20 columns including `user_id`, `tier`, `suggestions`, `polished_resume`, `target_score`. A representative insert was accepted by the schema and rejected only by the `auth.users` foreign key, as expected.

> **Not applied deliberately:** a blanket `supabase db push` would replay migrations containing bulk `INSERT` data and duplicate the catalogue. Each migration was applied and verified individually instead.

---

### DB-02 · `service_role` had no GRANT on `notifications` — **CRITICAL**
**Found:** the nightly deadline-alert cron returned **500 every night**. `/api/cron/notifications` does `throw notifError`, and the service role could not read the table.
**Cause:** `GRANT … ON public.notifications TO service_role` appeared in **no migration**. Never granted, so never worked.
**Fixed:** 10 Aug 2026.
```sql
GRANT SELECT, INSERT, UPDATE, DELETE ON public.notifications TO service_role;
GRANT SELECT, INSERT, UPDATE, DELETE ON public.profiles      TO service_role;
```
**Evidence** — the cron's exact two queries were replayed with the service-role key:
```
tracker query : OK — 0 rows due within 48h
notifications : OK — 0 existing DeadlineAlert rows
VERDICT       : cron would now succeed
```

---

### DB-03 · Storage bucket `resume-toolkit` did not exist — **CRITICAL**
**Found:** every resume-builder profile-photo upload returned 500. `app/api/resume/picture/route.ts:35` writes to a bucket that was never created.
**Fixed:** 10 Aug 2026, in `20260810120000_repair_resume_toolkit_objects.sql` — private bucket plus four owner-scoped policies on `storage.objects` matching the route's `<user_id>/pictures/<uuid>` path convention.
**Evidence:**
```
avatars           public=true
company-logos     public=true
report-evidence   public=false
resume-toolkit    public=false   ← new
resumes           public=false
```

---

### DB-04 · `certifications` table could not be created at all — **HIGH**
**Found while fixing DB-01.** The migration aborted with `42P17: generation expression is not immutable`.
**Cause:** the `fts` STORED generated column called `array_to_string(topics, ' ')`. A stored generated column requires an `IMMUTABLE` expression; `array_to_string` is only `STABLE`. The migration could never have succeeded anywhere.
**Fixed:** 10 Aug 2026. Removed `topics` from the `fts` expression and gave it a proper `GIN(topics)` index instead — the correct way to query an array. Verified no code reads `certifications.fts`: the page filters client-side over the fetched rows.

---

### DB-05 · `20260626000000_resume_toolkit_v2.sql` is unreplayable — **HIGH (structural)**
**Found:** its first statement is a bare `CREATE TABLE public.resumes` with no `IF NOT EXISTS`. Since `resumes` already exists, the statement fails and aborts the transaction before reaching the three objects that *are* missing. This is why DB-01 could never self-heal.
**Fixed:** 10 Aug 2026 by adding `20260810120000_repair_resume_toolkit_objects.sql`, which creates only the missing objects and is fully idempotent (`IF NOT EXISTS`, `DROP POLICY IF EXISTS`, `ON CONFLICT DO NOTHING`) so it is safe in every environment.

---

### APP-01 · `/resume/copilot` was a hardcoded mockup reachable by students — **CRITICAL**
**Found:** the "68" ATS score, the donut chart offset, "Last saved 2m ago" and the gap list were all literals in `app/(protected)/resume/copilot/page.tsx`. None of it came from the student's resume. Reachable from `/resume` via `resume-list-client.tsx:81`.
**Fixed:** 10 Aug 2026, in three parts — unlinking alone was not enough, because the route stayed directly reachable and would still have shown a fabricated score to anyone with the URL.
1. Removed the live entry point in `resume-list-client.tsx` (the only reachable one — `/resume` renders this component).
2. **Replaced the mockup itself** with an honest under-construction page that says where the feature stands and points to ATS Check and the Resume Builder. The route is kept, because the real optimiser will be built there.
3. Deleted `workspace-tools-panel.tsx` — dead code (zero importers) carrying the same link.

**Evidence:** `grep -rn "resume/copilot"` across `app`, `features`, `components`, `lib` now returns only an explanatory comment — no link remains. Production build compiles `/resume/copilot`; 254/254 tests pass; TypeScript unchanged at the 36 baseline. The new page was rendered and visually confirmed via a temporary unprotected route, which was then deleted.

> Judgement worth recording: a fabricated ATS score is worse than no score, because a student may rewrite their resume in response to it. That is why the mockup was replaced rather than merely hidden.

---

### OPS-02 · Nothing detected schema drift — **HIGH (prevention)**
**Found:** DB-01 through DB-05 went unnoticed for weeks. A missing table only surfaces as a 500 on the single code path that touches it, and nobody was on those paths. There was no check that the deployed database matched the code.
**Fixed:** 10 Aug 2026. Added `lib/schema-guard.ts` — `verifySchema()` probes 24 required objects (18 tables, 4 storage buckets, 2 RPCs) and reports each as `ok` / `missing` / `denied` / `error`.

Two design points, both learned from this incident rather than assumed:

1. **Existence is not enough.** `notifications` existed the whole time — only the GRANT was absent. The guard probes *reachability as the role that uses the object*, so it separates `missing` (needs a migration) from `denied` (needs a GRANT). Collapsing those into "broken" would send someone to the wrong fix.
2. **`head: true` count queries cannot prove existence.** PostgREST returns `count: null` with **no error** for a table that does not exist. A head-count probe gave a false all-clear during the audit itself and had to be re-run. Every check now issues a real row select and reads the error code. This is pinned by a test.

**Wiring:** `instrumentation.ts` runs it in development only, fire-and-forget — Next.js awaits `register()` before serving requests, so probing 24 objects there would add that cost to every production cold start for nothing. Production gets the same check on `/api/cron/health`, which already runs daily; it now returns `status: "degraded"` and **HTTP 503** when drift is present, with a per-object failure list.

**Evidence:** 12 new tests, all passing. Mutation-tested — disabling the `42501` branch made exactly the two GRANT tests fail, so they detect real regressions rather than passing vacuously. Run against the live production database: **all 24 objects `ok`**, independently confirming the DB-01–DB-05 repair holds. Suite 266/266, TypeScript unchanged at 36, lint clean on all four touched files.

---

### APP-02 · Resume Optimisation — generation wired, PDF export, full UI — **the handoff prompt's stated next task**
**Found:** `lib/resume-optimizer/generate.ts` (Resume A/B generation, fabrication-guarded) existed with zero importers. No route, no PDF export, `/resume/copilot` was still the honest placeholder from APP-01.

**Built:** 10 Aug 2026, in the order the handoff prompt specified.
1. **`lib/resume-optimizer/run.ts`** — orchestrates a run: `extractJDIntelligence` → `evaluateResumeEvidence` → `calculateAtsV2Score` for the real baseline → `decideTier` → `deriveSuggestions` → `generatePolishedResume` where the tier calls for it, scored again with `calculateAtsV2Score` (never the model's own claim). A generated resume that cannot be scored is not shown — an unscored resume presented as scored would be exactly the fabrication this feature exists to avoid.
2. **`POST /api/resume/optimization`** (start a run, persists to `resume_optimizations`) · **`GET /api/resume/optimization`** and **`GET /api/resume/optimization/[id]`** (list/fetch — this is what makes a run survive logout) · **`PATCH /api/resume/optimization/[id]`** (checklist toggle; triggers Resume B generation once `targetResumeUnlocked()` flips true; **un-checking a suggestion after Resume B exists clears `target_resume`/`target_score`**, since it would otherwise keep presenting un-confirmed work as done).
3. **`lib/resume-optimizer/pdf.tsx`** — ATS-safe PDF via `@react-pdf/renderer`: single column, no colour, no tables, Helvetica (no font embedding needed). Deliberately not built on the vendored `packages/pdf` (Reactive Resume fork) to stay clear of the unresolved AGPL question in LEGAL-01.
4. **`features/resume-toolkit/components/optimizer/`** replaces the `/resume/copilot` placeholder: intake form (saved resume or upload, JD/role/company), baseline score always shown, tier message, suggestion checklist with progress bar, Resume A/B cards with the download gate, and a past-runs list. Re-linked from `resume-list-client.tsx` (unlinked there since APP-01).
5. Added `resume_polish` / `resume_target` to `RATE_LIMITS` (10/day each) — the handoff flagged these as declared `AIFeature`s with no limit, i.e. unlimited. (The remaining unlimited features were then closed by SEC-01, below.)

**Evidence:**
- 11 new tests (9 orchestration in `tests/resume-optimizer-run.test.ts` — mocking the AI-facing calls to assert the tier/warning/fabrication-safe branches independent of what the real scoring engine returns for a fixture resume; 2 PDF in `tests/resume-optimizer-pdf.test.ts` — **round-trips the actual generated PDF bytes through `pdf-parse`** and asserts the candidate's name, company, bullet text and skills come back as real extractable text, not an image). Suite: **277/277**.
- `npx tsc --noEmit`: **36 errors — unchanged baseline.** `npx eslint` clean on every touched file.
- `npm run build`: passes; all five new routes (`/api/resume/optimization`, `/api/resume/optimization/[id]`, `/api/resume/optimization/[id]/download`, plus the rebuilt `/resume/copilot` page) compile and register correctly.
- UI rendered and screenshotted via a temporary unprotected route (both the intake form and, with a fixture run, the results/checklist/lock view), then the route was deleted — same method APP-01 used, for the same reason: the five real accounts in `auth.users` are not mine to sign into.

**Not verified — say so plainly:** no end-to-end run against a real logged-in session or the live AI gateway/provider chain — that needs a real account, which I did not create or sign into. The owner should run one real optimisation end-to-end (per the "dual verification" rule — UX is the owner's call) before this is called done.

**Deployed:** committed `3aa4612` and pushed to `restore-june19` (the production branch). Not verified against the live deployment beyond confirming the site still responds correctly post-push (`/resume/copilot` → 307, `/api/cron/health` → 401, both matching pre-existing behaviour) — that is not the same as confirming the new routes work in production, which needs the same real-session run as above.

**Completeness pass, 10 Aug 2026 — two real gaps found and fixed on re-review, not just polish:**
1. **`polish_only` tier's checklist implied a Resume B that would never exist.** `tierPlan('polish_only')` correctly returns `generatesTarget: false` — no second resume for that tier — but `generatesSuggestions: true`, so the UI still rendered the interactive "confirm each item to unlock Resume B" checklist even though there was no Resume B section anywhere on the page to unlock. Worse, the **PATCH route had no tier check at all** — confirming a `polish_only` checklist would silently trigger a real `generateTargetResume` AI call for a resume the UI had nowhere to show or download. Fixed both sides: the PATCH route now gates target generation on `tierPlan(run.tier).generatesTarget`, and the UI renders `polish_only` suggestions as a plain informational list ("Worth strengthening" — no checkboxes, no progress bar, no gate language) instead of the interactive checklist, which is now shown only for the `full` tier where Resume B actually exists.
2. **A `full`-tier run with zero suggestions could never get Resume B.** `targetResumeUnlocked([])` is `true` by design (nothing to confirm) — but Resume B was only ever generated from the checklist's PATCH trigger, and the checklist UI doesn't render when `suggestions.length === 0`. So a resume that scored under 80 overall but happened to have every individual requirement already evidenced would sit "unlocked" forever with no way to actually produce the unlocked resume. Fixed in `startOptimizationRun()`: when the tier is `full` and there are zero suggestions, Resume B is now generated immediately at start time, same scoring discipline as everywhere else (`calculateAtsV2Score`, not shown if it can't be scored).

**Evidence:** 2 new tests in `tests/resume-optimizer-run.test.ts` (zero-suggestions auto-generates Resume B; pending suggestions do not). Suite: **296/296**. TypeScript unchanged at 36. Lint clean. Re-verified the UI fix visually via the same temporary-unprotected-route method, confirming a `polish_only` fixture run renders "Worth strengthening" with no Resume B section anywhere on the page — checked via `get_page_text`, not just a screenshot glance. Route deleted after.

**Full production verification pass, 10 Aug 2026, before calling this done:**
- **Schema:** every column the code reads/writes on `resume_optimizations` confirmed present in production with the expected type via `information_schema.columns` — no drift between code and deployed schema.
- **RLS:** confirmed enabled (`relrowsecurity = true`) with all four policies (`SELECT`/`INSERT`/`UPDATE`/`DELETE`) present and each keyed on `auth.uid() = user_id`, read directly from `pg_policy`.
- **Routes live:** hit all five endpoints in production unauthenticated — `POST`/`GET /api/resume/optimization`, `GET`/`PATCH /api/resume/optimization/[id]`, `GET .../download` all return **401**; `/resume/copilot` returns **307** to login. Proves the deployment actually shipped and the auth boundary holds, not just that the build compiled.
- **Third bug found and fixed:** `resume_optimizations.original_resume_id` had `ON DELETE CASCADE` back to `resumes`, inherited from the original (pre-APP-02) migration. `deleteResume()` in `resume-actions.ts` performs a real hard `DELETE` on a saved resume — so a student picking "Saved Resume" to start a run, then later deleting that resume from `/resume`, would silently lose the **entire optimisation run**, including a completed, checklist-confirmed Resume B, even though `source_resume` is stored as its own JSONB snapshot specifically so the run doesn't depend on the original resume still existing (per that migration's own comment). Fixed with `supabase/migrations/20260810150000_fix_resume_optimizations_resume_fk.sql`: `ON DELETE SET NULL` instead. Applied directly to production and verified via `pg_constraint` (`confdeltype` changed from `c` to `n`). `user_id`'s own `ON DELETE CASCADE` was left alone — that one's correct, an account deletion should take its optimisation runs with it.
- **Schema guard:** re-ran `verifySchema()` against production after the constraint change — **all 24 objects still `ok`**.
- Full gate one more time: **296/296** tests, TypeScript unchanged at 36, production build clean.

**Route-handler test coverage added, 10 Aug 2026 — the largest remaining verification gap that didn't need a real session to close.** Everything up to here tested `lib/resume-optimizer/*` directly; none of the five Next.js route handlers themselves had ever been exercised — only traced by eye. That's exactly the layer where the `polish_only` and zero-suggestions bugs above were living, so eye-tracing had already proven insufficient once. Added `tests/api-resume-optimization.test.ts`, `tests/api-resume-optimization-id.test.ts`, `tests/api-resume-optimization-download.test.ts`, plus a shared `tests/helpers/fake-supabase.ts` (a minimal chainable fake of the Supabase query builder, so each test controls exactly what each table+operation returns without a real database). These call the actual exported `POST`/`GET`/`PATCH` functions with a real `NextRequest`, not a re-implementation of their logic.

**28 new tests**, covering per route:
- Auth boundary: every route 401s with no session, checked first, before touching validation or the database.
- Input validation: short JD, missing role/company, missing resumeId-or-resumeData, invalid `variant`.
- Ownership: `resumeId` lookups and run lookups are asserted to carry `.eq('user_id', user.id)` — not just RLS as the only line of defence, matching the project's existing "belt and suspenders" pattern elsewhere.
- The two bugs fixed above, re-proven at the HTTP layer: confirming a `polish_only` checklist (even fully unlocked) never calls `runTargetGeneration`; a `full`-tier confirmation does, and persists the result.
- Un-confirming a suggestion after Resume B exists clears `target_resume`/`target_score` in the actual persisted row.
- The download route streams **real PDF bytes** (not mocked — `renderResumePdf` runs for real) with the correct `Content-Type`/`Content-Disposition`, 404s the requested variant without silently substituting the other one, and a filename-sanitisation test using a `../../etc/passwd; rm -rf`-style resume name.
- Failure paths: orchestration failure → 502 without inserting a row; DB write failure after successful scoring → 500; list-query failure → 500.

**Evidence:** Suite: **324/324** (up from 296). TypeScript unchanged at 36. Lint clean on all 4 new files. Production build unaffected.

**What this does and doesn't close:** this proves the HTTP layer — auth, validation, the exact Supabase calls made, response shapes, and the tier-gating fixes — all wired correctly, using a fake database. It is not, and cannot be, a substitute for one real run through the real AI gateway with a real account — that remains the one thing only the owner can verify, per the project's own "dual verification" rule and the hard rule against signing into the five real `auth.users` accounts.

**4th bug — the one the owner actually hit, 10 Aug 2026: `/resume` (not `/resume/copilot`) had its own separate, pre-existing fake mockup.** The owner reported "the feature doesn't even open." All the verification above was against `/resume/copilot`, the route APP-01 fixed and APP-02 built out — but the main `/resume` landing page (`app/(protected)/resume/page.tsx`, last touched 24 Jul 2026, before this session and before the 10 Aug audit) had its own **completely independent** hardcoded "AI Resume Optimizer" panel sitting in the center column: a fixed **"68"** ATS score in a donut chart, three fabricated "Critical Gaps," a fake "Generated Result Preview," and two `<button>` elements — "Preview Full" and "Apply All AI Fixes" — with **no `onClick` handler at all**. The sidebar's "AI Optimizer" card was a plain `<div>` with a pointer cursor and no `href`. A student landing on `/resume` (the normal way in) would see what looks like a working feature with a real-looking score, click a button, and nothing would happen — exactly what was reported, and exactly the class of fabricated-data bug this whole project exists to eliminate. The 10 Aug audit never flagged this because it audited `/resume/copilot` specifically; this file was never in scope for APP-01 and I missed it too, despite multiple "thorough" review passes, because I was verifying the route I built rather than checking whether another entry point existed.

**Fixed:** 10 Aug 2026. Replaced the fake donut/gaps/preview panel with an honest card (no fabricated numbers, real copy explaining what the feature needs from the student) linking to the real `/resume/copilot`. Made the sidebar card a real `<Link href="/resume/copilot">`. Verified via the same temporary-unprotected-route method — screenshotted the new card, then clicked the real rendered link (not just checked the `href` attribute) and confirmed it navigated to `/resume/copilot`, which correctly redirected to `/login`. Route deleted after. TypeScript unchanged at 36, lint clean, 324/324 tests, build clean.

**Not yet checked:** the same page's right-column "Career Insights" panel (`+12% this month` resume strength, a fixed skills list, "342 active roles match your profile") is *also* entirely fabricated and untouched by this fix — it wasn't what was reported broken, and touching it wasn't requested, but it's the same class of problem sitting on the same page and is worth a follow-up.

**5th bug — the entry point opened, but analysis never completed, 10 Aug 2026.** With the 4th bug fixed, the owner reached the real `/resume/copilot` page, filled in the form, and submitted — and it never analysed. Root cause: **neither `POST /api/resume/optimization` nor `PATCH /api/resume/optimization/[id]` declared `maxDuration`.** A full run makes up to ~6 sequential AI-gateway calls (JD extraction, baseline evidence eval, polish generation with its own guarded retry, polish evidence eval, and conditionally a target generation + eval). `lib/ai-gateway/index.ts`'s own per-provider timeouts run up to 40s (Ollama) and the gateway falls back through several providers on failure, so a worst-case run legitimately takes well over a minute. Every other AI-heavy route already built this session (the two cron routes) explicitly sets `maxDuration = 300`, matching the pre-existing `refresh-employers` cron's own documented reasoning — these two routes were the ones that didn't get it, because they route through library orchestration (`startOptimizationRun`/`runTargetGeneration`) rather than an obviously-slow scrape loop, which is presumably why it wasn't obvious at build time that they needed it too.

**Fixed:** 10 Aug 2026. Added `export const maxDuration = 300` to both routes, with the reasoning written inline so it isn't lost again. Also updated the UI's loading toast to say "this can take up to a minute" rather than a bare "Scoring your resume...", since a legitimately-slow-but-working request should not read to a student as a frozen page. 324/324 tests, TypeScript unchanged at 36, lint clean, build clean.

**Not independently confirmed as the sole cause — say so plainly.** I don't have Vercel log access (not authenticated in this environment), so I could not read the actual error/timeout that occurred during the owner's real attempt. This fix addresses a real, structural gap that I'm confident was at minimum contributing, and matches the exact symptom reported (upload and form entry worked; submission produced nothing). If the owner tries again and it still doesn't analyse, the next diagnostic step is the browser Network tab's response for the `POST /api/resume/optimization` call, or the Vercel dashboard's function logs for that route.

**6th bug — the actual dominant cause, found by directly running the real pipeline against the owner's own resume and JD, 10 Aug 2026.** Ran `startOptimizationRun` directly (via `tsx`, real provider keys, no login needed) with the owner's exact resume content and job description. It completed correctly in 43.8s — proving the AI gateway and orchestration were never the problem. The real bug was upstream: **`resumes.parsed_data` is stored in the Resume Builder's shape (Reactive Resume's `ResumeData` — `basics.name`, `sections.experience.items[]`, HTML descriptions), not the flat `ParsedResume` shape the scoring engine reads (`name`, `skills[]`, `experience[]`).** Both `POST /api/resume/optimization` (for the "Saved Resume" path — the one the owner actually used, confirmed from their screenshot) and the pre-existing `/api/resume/ats-check` were casting `resumes.parsed_data` straight to `ParsedResume` with **zero conversion**. The AI was receiving an object with `name`/`skills`/`experience` all `undefined` — evidence evaluation correctly found nothing to evaluate, since there was genuinely nothing there. `ats-check`'s own `sample-frontend-dev` hardcoded fixture is in the correct flat shape — that's very likely the only path anyone ever actually exercised, which is why this went unnoticed.

The "Upload PDF" path had the same root problem for a different reason: `/api/resume/parse` (the Resume Builder's own extractor, which the optimiser's upload path reused) also returns `ResumeData` shape — and independently, that endpoint's extraction turned out to be unreliable regardless of shape. Traced directly: it asks the model to produce Reactive Resume's full schema (picture / metadata / layout / 12 section types) described only in prose — the schema itself is never actually shown to the model. A real extraction attempt on the owner's resume came back as `{"id": "..."}` and nothing else.

**Fixed:** 10 Aug 2026, three parts.
1. **`lib/resume-optimizer/convert-resume-data.ts`** — a real converter from the Builder's `ResumeData` shape to `ParsedResume`, handling HTML-to-text (turning `<li>` into separate bullets), splitting `"May 2026 - Aug 2026"`-style periods into start/end, classifying degree level from free text, and — deliberately — only setting a GPA when the source is unambiguously a 0-10 scale value (`"9.28/10"`); a percentage like `"94.2%"` is left out rather than converted, since that would be inventing a number. Wired into `POST /api/resume/optimization` for the `resumeId` path, with a guard (`looksLikeParsedResume`) so an already-flat resume is never double-converted.
2. **`lib/resume-optimizer/extract-resume.ts` + `POST /api/resume/optimization/extract`** — a new, dedicated extraction path for the optimiser's "Upload PDF" flow that targets `ParsedResume` directly instead of routing through the Builder's fragile schema. Deliberately not a fix to `/api/resume/parse` itself — that endpoint is shared by the Resume Builder and `ats-check`, and rewriting its prompt risked breaking those without being able to verify either live. The frontend now calls the new endpoint instead.
3. **Two schema bugs found via the real extraction, both silently failing the entire resume over one field:** `ResumeProjectSchema.technologies` had no default, so a model correctly omitting it for a project with no separately-listed tech stack failed validation for the *whole resume* — given `.default([])`. And a model instructed to omit an unavailable GPA was instead writing `gpa: 0` (a false claim, not "unknown") — now stripped in `extractResumeFromText`.
4. **Added `certifications`/`achievements` to `ParsedResumeSchema`** (`.optional()`, not `.default([])`, to avoid forcing every existing literal `ParsedResume` object across the codebase to add them) — the evidence evaluator had no clean signal to distinguish "certified in X" from "built a project in X" without them. Wired into both the new extractor's prompt and the Builder converter (pulling `sections.certifications`/`sections.awards`).

**Evidence:** ran the real pipeline end-to-end against the owner's actual resume PDF and actual job description (not a synthetic fixture) — PDF → real text extraction → new dedicated AI extraction → conversion → full `startOptimizationRun`. Final extraction captured all 5 certifications, all 4 achievements, all 6 experience bullets, all 4 projects, correct GPA (9.28, with no fabricated `0` for the two school levels that only had percentages) — verified by reading the actual JSON output, not just checking it didn't throw. 18 new tests (`resume-optimizer-convert-resume-data.test.ts`, `resume-optimizer-extract-resume.test.ts`) plus 2 existing tests fixed (they used `resumeData: {}` as a placeholder, which a new, correct validation now rejects — updated to a realistic fixture rather than loosening the check). Suite: **342/342**. TypeScript unchanged at 36. Lint clean.

**Also found, not fixed at the time — separate and pre-existing:** `ats-check`'s `resumeId` path has the exact same `parsed_data as any` cast bug. Out of scope here (touching that route risked breaking a feature I couldn't live-verify), logged for follow-up. **Fixed in APP-04** (below) once the ATS Checker itself was in scope. Native Gemini access is currently returning `403 Forbidden — "Your project has been denied access"` (not just the earlier `429` quota message) — worth checking the Google Cloud project's status directly; the gateway's fallback to OpenRouter/Groq/Cloudflare already covers this, as proven by every test above succeeding despite it.

---

### APP-03 · ATS scoring read as keyword matching, not recruiter judgment — **owner-requested, 10 Aug 2026**
**Found:** the owner gave concrete, worked examples of what the scoring engine should — and did not — distinguish: "certified in CSS" vs "built a project using CSS" vs "learned CSS" should not score the same; a virtual/simulated internship vs a real one should not score the same; a toy project (to-do list, calculator) vs a production-scale one (the owner's own "Opportunity Radar", or comparable system-design-level work) should not score the same. The scoring math (`lib/ats-checker/evidence-scoring.ts`) already had a `TYPE_BONUSES` table differentiating evidence types, but `certification` and `project` were tied at the same value (0.08), and — the larger gap — the evidence-evaluation prompt (`ats-v2-prompts.ts`) never instructed the model to judge a project's own complexity/scale at all, so two very different projects on the same technology could receive identical `evidenceStrength`. Separately, `gapReason` (the field that becomes the suggestion checklist's actual advice text — see `deriveSuggestions` in `tiers.ts`) is a valid, optional schema field the prompt's example output never showed populated, so it was very likely always empty, and every suggestion was silently falling back to `tiers.ts`'s generic templated sentence instead of real feedback.

**Fixed:** 10 Aug 2026.
1. Rewrote the evidence-evaluation system prompt with an explicit calibration hierarchy: bare skill listing < certification/coursework (capped at "moderate" — proof of studying, not building) < project (scaled by the project's own complexity — toy-scale caps at "moderate", production-shaped can reach "exceptional") < professional experience (scaled by how concrete and quantified the description is, not by the job title alone — a vague "virtual internship" caps at "moderate" the same as a toy project).
2. Widened `TYPE_BONUSES`: `certification` 0.08 → 0.05, `project` 0.08 → 0.09, so a certification can no longer numerically tie a real project on the same skill.
3. Made `gapReason` a required, worked-example field in the prompt's output schema whenever `satisfaction` isn't `"complete"`, with explicit instructions to write it the way a mentor would — specific and actionable, not a restatement of the requirement.
4. Added `certifications`/`achievements` to `ParsedResumeSchema` (see APP-02 above) specifically so this evaluator has a clean signal for "certified" that isn't inferred from a skill string.

**Evidence — a live, real AI proof, not just prompt text review.** Built one test resume with three parallel cases against a real JD: CSS backed only by a certification, HTML used only in a "Personal To-Do List" toy project, and PostgreSQL used in a production-shaped rebuild of the owner's own "Opportunity Radar" description (4,700+ records, multi-provider pipeline, deduplication engine). Ran the actual `evaluateResumeEvidence` → `calculateAtsV2Score` pipeline with real provider calls (Gemini was 403'd during this run; OpenRouter served it, proving the fallback path too):

| Case | satisfaction | evidenceStrength | score |
|---|---|---|---|
| CSS — certification only | partial | weak | **45** |
| HTML — toy project | partial | moderate | **59** |
| PostgreSQL — production-scale project | complete | exceptional | **100** |

Exactly the ordering requested: certification < toy project < production project. `gapReason` for the two unmet cases came back specific and actionable — e.g. *"A project showcasing more advanced CSS techniques (e.g., responsive layouts, animations, or component styling) would strengthen this area"* — not the old generic fallback. Also added 8 new deterministic unit tests for `scoreRequirement` (`tests/ats-evidence-scoring.test.ts` — no prior coverage existed for this function at all), pinning the certification-never-outscores-project invariant, the quantified-impact bonus, importance weighting, and satisfaction-level monotonicity, so this doesn't silently regress. Suite: **350/350**. TypeScript unchanged at 36. Lint clean.

**Not changed:** `lib/resume-optimizer/generate.ts`'s writing rules (banned AI-tell words, plain strong verbs) already covered the "professional voice / power words" request reasonably well on review — left alone rather than risk destabilizing an already-tested, working prompt.

---

### APP-04 · ATS Score Checker ran two competing scoring engines and persisted the wrong one — **owner-requested, 10 Aug 2026**
**Found:** a full audit of every ATS-scoring code path (owner-requested: "trace the complete flow... audit the existing codebase before changing code") turned up that `/api/resume/ats-check` ran the deterministic, evidence-based V2 engine (`extractJDIntelligence` → `evaluateResumeEvidence` → `calculateAtsV2Score`, the one `/resume/copilot` also uses) as its stated "PRIMARY" pipeline, but then — whenever V2 succeeded — unconditionally ran a **second, independent, keyword-matching engine** (`lib/ats-checker/job-match.ts` + a separate legacy JD-extraction prompt) in the same request, producing a second, different score for the same resume+JD. The client picked whichever score it found first for display, but the **database write always used the legacy engine's score, never V2's** (`score: jobMatchResult ? jobMatchResult.score : readiness.score` — `atsV2Data.score` was never referenced), so the stored `resume_ats_reports.score` could permanently disagree with what the student was shown on screen. A third, fully orphaned prompt (`services/ai/prompts.ts`'s `atsCheckSystemPrompt`) that literally asked the LLM to invent and return a `"score": 0-100` itself — the exact anti-pattern the owner explicitly ruled out — sat unused in the codebase with a matching dead schema vendored into `packages/schema/`, a landmine for a future accidental reconnect.

Separately, when the V2 pipeline itself failed (JD extraction or evidence evaluation), the route set one hardcoded banner text ("unable to extract meaningful requirements... after all fallback attempts") **regardless of which stage actually failed or why**, while still rendering a JD-independent "Readiness Metrics" card immediately below it with no framing — reading as if a complete analysis had happened next to a claim that it hadn't. And the Resume Optimiser's evidence prompt had exactly one few-shot example (a CSS-certification case); live testing showed the model pattern-copying that example's exact sentence structure across unrelated skills — "You've listed X as a skill, but nothing on your resume shows you've actually built something real with it," verbatim, for HTML then CSS then JavaScript then Bootstrap then React — which is what produced the near-identical "Build a project using X" checklist spam the owner reported.

A separate, previously-logged-but-deferred bug (`ats-check`'s `resumeId` path casting `resumes.parsed_data` — stored in the Resume Builder's nested `basics`/`sections.*.items[]` shape — directly to the flat `ParsedResume` shape every scoring function reads) was also still live: the exact bug already found and fixed in the Optimiser in APP-02, never applied here. The ATS Checker's "Upload PDF" path also still called `/api/resume/parse` (the Builder-shape extractor, already known unreliable for this purpose — see APP-02) instead of the dedicated flat extractor the Optimiser was switched to.

**Fixed:** 10 Aug 2026.
1. Deleted the entire legacy engine: `lib/ats-checker/job-match.ts`, the legacy JD-extraction prompt, the orphaned score-inventing prompt (`prompts.ts`) and its dead vendored schema duplicate (`packages/schema/src/resume/ats-check.ts`), and `jobMatchResultSchema`/`JobMatchResult`. One engine now: AI extracts structure and evidence, `calculateAtsV2Score` (pure TypeScript, zero AI involvement) computes the number.
2. Applied the APP-02 Builder-shape conversion (`convertResumeDataToParsedResume`/`looksLikeParsedResume`) to both the `resumeId` and upload paths, and switched the upload path to `/api/resume/optimization/extract` (the dedicated flat extractor), matching the Optimiser exactly.
3. Made the job description optional: `mode: 'resume_only'` (deterministic `calculateAtsReadiness`, no AI, no JD needed) vs `mode: 'targeted'` (the V2 engine), explicit in both the response schema and the UI — the owner's requirement that these be "two distinct, clearly-labeled modes," not one silently degrading into the other.
4. Replaced the boolean `aiFailed` with a structured `analysisError: { stage: 'jd_extraction' | 'evidence_evaluation' | 'unexpected', message }`, so the banner shows the AI gateway's actual returned reason instead of one hardcoded sentence regardless of cause. Readiness is still shown on failure, now explicitly labeled "does not depend on this job description" rather than sitting under the failure banner unexplained.
5. Extracted the gap → checklist deriver (`deriveSuggestions`) out of the Optimiser into `lib/ats-checker/gap-suggestions.ts`; both `/resume/ats` and `/resume/copilot` now call the identical function against the identical `StructuredJD`/`EvidenceMatrix`, so the two features can no longer disagree about what's missing for the same resume+JD.
6. Moved the CGPA/academic-standing rule out of the AI-coaching branch into a pure, directly-testable function (`lib/ats-checker/academic-recommendation.ts`) exposed as a top-level `academicRecommendation` field — mode-agnostic, no longer smuggled inside a coaching AI call's output.
7. Rewrote the qualitative coaching call (`buildAtsCoachingPrompt`) to take the already-final V2 result as input and produce narration only (`recruiterVerdict`, `powerWords`) — explicitly forbidden from recomputing or contradicting the score, evidence, or checklist, closing the "no competing logic between components" gap for the last AI surface in the pipeline. If this narration call fails, it is simply omitted — never replaced with fabricated fallback text standing in for something the AI didn't actually say.
8. Hardened both V2 prompts based on what the audit found: the JD-extraction prompt now explicitly instructs extracting *every* distinct requirement ("a typical real JD yields 8-20; extracting 1-2 almost always means you stopped too early") with explicit preferred-vs-required category guidance; the evidence prompt gained an explicit anti-templating rule plus a second, structurally different few-shot example, so the model reasons per-requirement instead of copying one example's phrasing.
9. Fixed the DB persistence bug directly: `resume_ats_reports.score` is now always `atsV2Data.score.overallScore` when targeted analysis succeeds (or `readiness.score` in resume-only mode) — never a second engine's number.
10. Added `export const maxDuration = 180` — this route had none at all despite making up to 3 sequential multi-provider AI calls, the same class of bug already found and fixed on `/api/resume/optimization` (silently killed by Vercel's default timeout mid-pipeline).

**Evidence — live, real AI proof, not just prompt text review.** Ran the actual `extractJDIntelligence` → `evaluateResumeEvidence` → `calculateAtsV2Score` → `deriveSuggestions` pipeline via `tsx` against real provider calls (OpenRouter/Groq served it; native Gemini is still 403'd, as previously logged):
- **JD-extraction breadth regression caught live, then fixed live:** the first run against a real, 19-line JD extracted **1 requirement**, reproducing the exact "no meaningful requirements" failure mode from the bug report even though the JD plainly listed a dozen. Root cause: `extractJDIntelligence`'s validator only rejected *zero* requirements, so a technically-non-empty but badly under-extracted response passed validation and never retried across the provider chain. Added a length-proportional minimum requirement count to the validator (`lib/ats-checker/ats-v2-intelligence.ts` — 3 for a >300-char JD, 5 for >900 chars) forcing retry on under-extraction. Re-ran against the same JD: **19 requirements** extracted, correctly split into `technical_capability`/`responsibility`/`education`/`location_auth`/`preferred_qualification` categories (4 correctly identified as preferred-only: TypeScript, Docker, Kubernetes, prior internship).
- **Anti-templating fix confirmed live:** evaluated a skills-only-no-evidence resume against those 19 requirements — 16 gap reasons came back with 16 distinct openings (previously the exact bug: near-identical phrasing across every skill), each citing the resume's own specifics (e.g. *"While your portfolio shows basic UI creation, this role requires..."* vs *"This role requires the ability to integrate REST APIs, but your resume doesn't show any experience with this."*).
- **Evidence-based discrimination confirmed live, same JD, two resumes:** a skills-only-no-evidence resume scored **34 (poor)**; a resume with a genuine production-shaped project and real experience bullets against the identical JD scored **70 (moderate)** — a 36-point spread from evidence quality alone, with only Docker/Kubernetes (genuinely absent) left as gaps for the strong resume vs. 6 gaps for the weak one.
- **Malformed JD failure confirmed clean:** fed 180 characters of non-JD noise text — failed across all 6 configured providers (the validator correctly rejecting each attempt), returned `success: false` with the real reason, no fabricated score at any point.
- **Academic recommendation confirmed:** correctly triggered for a 7.1 GPA in-progress B.Tech resume, correctly returned `null` for an 8.4 GPA one on the identical rule.
- Both `/resume/ats` UI states (targeted success, targeted failure, resume-only) and the intake form (JD-optional, dynamic Target Role/Company enabling, dynamic button text) were screenshot-verified via a temporary unprotected route, then deleted.
- 5 tests updated for the new contract (CGPA assertions moved to `academicRecommendation`, failure-mode tests now assert the real `analysisError.stage`/`message`), 2 legacy-only test files deleted (`ats-scoring-audit.test.ts`, `ats-scoring-v2.test.ts`), 5 new tests added (resume-only mode never touches the V2 pipeline, DB-persisted score matches the response exactly, JD-extraction-prompt content). Suite: **342/342** (350 baseline − 11 legacy-only tests deleted with their engine + 3 new). TypeScript unchanged at 36. Lint: error count on touched files actually *dropped* (24→9) from deleting legacy code; remaining `any` casts match the codebase's existing Supabase-JSONB convention. Production build passes, `/resume/ats` and `/resume/copilot` both present as dynamic routes.

---

### APP-05 · Hub — global community chat built out and fixed — **owner-requested, 11 Aug 2026**
**Found:** substantial WIP Hub code already existed uncommitted (messages table + RLS, send/list/delete API routes, a realtime hook, message/profile/reply components) — functionally close to complete, but with real, concrete bugs and gaps:
- `hub/page.tsx` redirected unauthenticated users to `/auth/login`, a route that does not exist (every other protected page in the app uses `/login`) — would 404 instead of reaching sign-in.
- The `hub_messages` table's RLS only had SELECT/INSERT/DELETE policies; the grants migration granted UPDATE at the table level but no row-level policy ever permitted it, so editing (once built) would have silently failed under RLS regardless of the application code.
- `hub-profile-modal.tsx` used `max-w-md`, hitting the same Tailwind theme-collision bug already found and worked around once this session (UI-03: `--spacing-md` in `globals.css` silently resolves `max-w-md` to 16px) — the profile card was rendering as an unreadable 16px-wide sliver.
- `HubProfileModal`'s `getPublicProfile()` call had no error handling — a thrown error (RLS denial, transient DB error) became an unhandled promise rejection and left the modal stuck on "Loading profile..." forever, since `setIsLoading(false)` was never reached.
- The message action menu (Reply/Edit/Delete) was hover-only (`group-hover`), which never triggers on a touch device — on a chat feature, where mobile is the primary way anyone uses it, those actions were simply unreachable there.
- `hub-message.tsx` called `toLocaleTimeString()` directly during render (including SSR) — timezone/locale differ between the server and the viewer's browser, causing a real, reproducible hydration mismatch on every single message.
- A `next/image fill` image bubble used `w-full` inside a flex-col ancestor with no definite width of its own; since `fill` makes the `<img>` absolutely positioned (contributing zero intrinsic size to its parent), the whole image bubble collapsed to a single-digit-pixel dot.

**Built/fixed:** 11 Aug 2026.
1. Fixed the `/auth/login` → `/login` redirect bug, and moved `/hub` from the `(protected)` layout group (dashboard sidebar + header squeezing the chat into a narrow column) to `(protected-fullscreen)` (the same group Resume Builder uses) — a chat needs the full viewport, not a sidebar-constrained panel. Added a back-to-dashboard link into the Hub's own header to replace the navigation chrome that move removes.
2. Migration `20260811120000_hub_images_and_edit.sql`: added `image_url`/`image_width`/`image_height`/`edited_at` columns, relaxed the content check constraint to allow image-only messages, added the missing `hub_messages_update` RLS policy, and created the public `hub-attachments` storage bucket (same per-user-folder-ownership policy pattern as `avatars`). Applied directly to the linked production database via `supabase db query`, verified live (columns, policy, bucket all confirmed present via direct query).
3. New features beyond the original WIP, per the owner's explicit ask for "maximum number of features" on a WhatsApp-style chat: image sending (`POST /api/hub/upload`, with a client-side image picker + caption + upload progress state), message editing (`PATCH /api/hub/messages/[id]`, RLS-enforced to the sender, cannot edit a text-only message down to empty), a full-screen image lightbox with download, and a Supabase Presence-backed "N online" indicator in the header.
4. Fixed all bugs found above: `/login` redirect, the missing UPDATE RLS policy, the `max-w-md` collision (same inline-style workaround as before, now logged as a second occurrence of UI-03), the unhandled-rejection/stuck-loading bug in the profile modal (wrapped in try/catch, falls back to the basic sender info already on hand), the hover-only action menu (now also tap-to-toggle with click-outside-to-dismiss, repositioned above the bubble rather than to its side so it can't clip off a narrow phone screen), the timestamp hydration mismatch (deferred to a client-only effect, matching the established `typeof window` convention already used elsewhere in this codebase for the same class of problem), and the collapsed image bubble (fixed pixel width instead of a percentage one).
5. Full visual redesign pass across the header, message bubbles, input bar, and profile modal for a cleaner, denser, more professional feel, addressing the owner's explicit "the UI is not at all good" complaint.
6. **Found via the owner's own live test, after initial ship — every sender showed as "Student" with a generic avatar, and the profile modal looked incomplete.** Root cause: `profiles` SELECT RLS is `auth.uid() = id` — a user can only read their *own* row through the regular Supabase client. Two of the four sender-resolution code paths used the regular (RLS-bound) client to look up *other* users' profiles for a public chat where every member's name/avatar must be visible to everyone: `hub/page.tsx`'s SSR fetch (both the main sender loop and the reply-preview lookup), and `use-hub-messages.ts`'s realtime handler (client-side, so it could never legitimately use the service-role key directly). Confirmed live against production: a direct query returned the correct `name`/`avatar_url`/`bio`/`skills`/etc. for the real accounts involved, proving the data was always fine and the bug was purely in which client fetched it. Fixed by switching the SSR page to the service-role client (matching the pattern the API routes already used correctly) and adding a new `GET /api/hub/senders?ids=...` route so the realtime path can resolve other members' public info through an authenticated server endpoint instead of either being blocked by RLS or holding the service-role key in the browser. Re-verified live: the new route correctly resolves both real test accounts' full profile data (name, avatar URL, LinkedIn) against the production database.

**Evidence:** every interactive path — text send, image send with caption, reply, reply-preview-click-to-scroll, edit (save/cancel), delete, profile modal (including the graceful fallback when the deeper profile fetch fails), image lightbox open/close, tap-to-reveal action menu, mobile viewport (375px), and the resume-only readiness path — was exercised via a temporary unprotected preview route with realistic fixture data (text, image, reply, edited, own vs. other, consecutive-message grouping, failed/optimistic states), inspected via direct DOM/computed-style assertions (not just screenshots — several of the bugs above, including the collapsed image, the squished modal, and the stuck-loading profile, were only conclusively confirmed this way), then the route was deleted. The sender-resolution fix (item 6) was additionally verified with a live integration test calling the real `/api/hub/senders` route against the actual production database and real account data, then deleted. 342/342 tests (2 pre-existing `schema-guard.test.ts` fixtures updated for the new `hub-attachments` bucket entry). TypeScript unchanged at 36 baseline (some untracked root-level `test-hub-*.ts` debug scripts from the prior WIP session have pre-existing errors of their own — left untouched, not part of this change). Lint clean on every touched/new file. Production build passes with all five Hub API routes present (`messages`, `messages/[id]`, `send`, `upload`, `senders`).

**Not done — flagged, not fixed:** the app's dark mode (`.dark` class-based, `--color-surface` etc. redefined inside a `.dark {}` block in `globals.css`) did not visibly re-theme when tested — same pre-existing, wider app-level theming gap already noted once earlier this session on the Resume Optimiser pages, not something introduced or worsened here, and out of scope for a single feature's UI pass.

---

### APP-06 · Resume ATS Checker / Optimiser "stuck waiting" report — investigated, no pipeline bug found, one defensive fix shipped — **owner-reported, 11 Aug 2026**
**Found:** after the APP-04 rewrite shipped, the owner reported the ATS Score Checker was "just waiting" with no result — reported again after Vercel Deployment Protection (a separate, already-diagnosed blocker) was turned off, so a second cause was still in play. Given the Hub RLS bug found the same day (APP-05, item 6), the same class of bug — a route silently blocked by RLS or a service-role misconfiguration that only shows up in production, not local testing — was the leading suspicion and had to be ruled in or out directly against production, not assumed away.

**Investigated:** 11 Aug 2026, live against the real production database and the owner's own real saved resume ("Detailed_ATS_Test_Resume_Frontend"), not synthetic fixtures:
- Confirmed the deployment is no longer behind Vercel's own auth wall (`opportunity-radar-nc4d4cg9w-...`'s `/resume/ats` now correctly reaches the app's own `/login`, not a Vercel login page).
- Grepped every ATS/Optimiser route and page for `.from('profiles')` — zero matches outside `resume/save`. These are single-user features (a user only ever reads their own resume), so the Hub bug's specific root cause (RLS blocking a *cross-user* read) does not apply here; there is no equivalent code path that needs a service-role client and doesn't already have one.
- Ran the actual `POST /api/resume/ats-check` and `POST /api/resume/optimization` route handlers (not just the underlying library functions) against the real production database, real AI providers, and the owner's real saved resume + a real job description, with only the auth session mocked. Both completed successfully: ATS-check in 23.4s (score 96, band "exceptional", readiness 92 — all categories populated and sensible for a strong resume), Optimisation in 15.6-21.9s (baseline score 94-95, tier `already_strong`, correctly generating no polished/target resume per the tier's own documented "don't rewrite an already-strong resume" rule). Test rows created during this run were deleted from `resume_optimizations` and `resume_ats_reports` afterward so they don't appear as confusing history in the owner's own account.
- `vercel.json` defines 7 cron jobs — Vercel's Hobby tier caps cron jobs at 2, so this project almost certainly already runs on a paid tier where the function-duration ceiling is well above the ~15-25s these routes actually take, weakening (though not fully eliminating, since the plan tier was never directly confirmed) the "Hobby plan hard-caps function duration at 10s" hypothesis floated earlier.

**Conclusion:** no bug found in the pipeline itself — it is correct, and fast enough in practice, against real production data. The most likely explanation for the original report is the already-fixed Deployment Protection wall, tested again before the fix had time to take effect or propagate to a fresh deployment.

**Fixed regardless — a real, independent gap:** neither intake form (`ats-checker/index.tsx`, `optimizer/index.tsx`) had a client-side request timeout. A `fetch()` with no `AbortController` deadline is indistinguishable, from the user's seat, between "the server is still working" and "the connection silently died" — both just look like an infinite spinner. Added `lib/fetch-with-timeout.ts` (a thin `fetch()` wrapper with an `AbortController` and a clear, specific error message) and wired it into both forms' main analysis request, with a client-side ceiling set just under each route's own server-side `maxDuration` (170s / 290s) so a genuine hang now surfaces a real, readable error instead of an unbounded wait — independent of whatever the original root cause turns out to be.

**Evidence:** 3 new unit tests for `fetchWithTimeout` (resolves normally, aborts with a clear message past the deadline, re-throws a non-abort error unchanged) using fake timers, no real network calls. 345/345 tests, TypeScript unchanged at 36 baseline, lint clean on new/touched files (pre-existing `any`/`let` issues on untouched lines in the same files left as-is, matching established convention). Production build passes.

---

### APP-07 · Resume Optimiser hardening pass — 5 real bugs found and fixed live against production — **owner-reported, 11 Aug 2026**
**Found (owner, live-tested):** checklist stuck at 4/5 with no feedback; the same resume scored 92 in the ATS Checker and 74 in the Optimiser for the same job description ("very unprofessional... make sure it is not word matching"); Resume A ("polished") failing with "we could not generate a version we were confident was accurate"; suggestions gave no actionable "how" (just a gap reason); and the "Career Insights" rail on `/resume` showed hardcoded fake data (React.js/UX Design/Typescript/Figma, "+12% this month", "342 active roles") for every user regardless of whether they had ever uploaded a resume.

**1. Checklist silently hung.** `toggleSuggestion` in `optimizer-results.tsx` used a plain `fetch` with no timeout and no loading state. Confirming the *last* unconfirmed item synchronously triggers Resume B generation server-side (2 sequential AI calls, up to ~80s) — with nothing on screen to say so, it read as frozen. Fixed: `fetchWithTimeout` (290s on the unlock call, 20s otherwise), a spinner + "Saving..." on the pending item, and a `toast.loading` specifically when the confirm is about to trigger generation.

**2. The 92-vs-74 split — a real scoring-consistency bug, not word matching.** Reproduced live: ran the exact same resume + JD (pulled from the user's actual `resume_optimizations` row `7aa37efb`) through `extractJDIntelligence` → `evaluateResumeEvidence` → `calculateAtsV2Score` twice, independently. One pass extracted 5 requirements from the JD (all matched → 92); the Optimiser's earlier pass had extracted 13 (several gaps → 74). Root cause: the JD-extraction validator's under-extraction floor (`ats-v2-intelligence.ts`) capped at "≥5 requirements" for *any* JD over 900 characters, so a 2,315-character JD with ~15 genuine bullet requirements could under-extract to exactly 5 and still pass validation, never retrying through the provider chain. Fixed: extended the floor (`>2000 chars → ≥8`, `>1200 → ≥6`) and dropped extraction temperature from 0.1 to 0.0 to match the evaluation call. Re-ran the same live reproduction after the fix: two independent extractions now return byte-identical output — 25 requirements, score 36 both times (delta 0). The score dropped because full extraction surfaces real gaps (this JD wants PHP/C#/Java/backend/databases a frontend-only resume doesn't show) that the under-extracted 5-item version never asked about — 36 is the honest number, not a regression.

**3. Resume A/B generation failures — a real AI Gateway bug, not the fabrication guard.** Reproduced live: generating a polished resume for the same run fell through the entire provider chain — Gemini (403, project access denied), OpenRouter (402, insufficient credits), Groq (429, daily token quota exhausted) — down to the Cloudflare Workers AI fallback. Cloudflare's response parser did `String(rawRes)` on the model's output; for this model under JSON-mode, Workers AI returns `response` as an already-parsed object, and `String(object)` produces the literal text `"[object Object]"`, which every downstream `JSON.parse()` then rejects — a real generation success turned into a hard failure. Fixed: `JSON.stringify(rawRes)` instead of `String(rawRes)` when the response isn't already a string (`lib/ai-gateway/providers/cloudflare.ts`). Re-ran the same live reproduction after the fix: generation succeeds, fabrication guard reports clean, zero findings. **Flagged separately, not code-fixable:** the Gemini and OpenRouter failures are account/billing issues (dead API key/project and depleted credits respectively) — every AI-backed feature in the app is currently one provider (Groq) and one weak fallback (Cloudflare's 8B model) away from failing across the board whenever Groq's daily quota is also exhausted. Needs the account owner to check Gemini's Google Cloud project status and top up OpenRouter credits.

**4. No actionable guidance on suggestions.** `deriveSuggestions()` only produced a generic AI-written gap reason ("the JD asks for X, your resume doesn't show it") — no suggested project, tech stack, course, or study plan, despite the user asking for exactly that repeatedly. Added a deterministic `guidance` field per suggestion type (`lib/ats-checker/gap-suggestions.ts`): project suggestions now name a concrete build brief plus up to 3 sibling technologies pulled from the same JD's other requirements; course/certification suggestions name where to look; skill suggestions give a short practice plan. Deterministic rather than another AI call — the checklist already has everything needed, and a second AI round trip is exactly the kind of extra failure surface bug #3 above is a live example of. Rendered under each suggestion in both the ATS Checker and the Optimiser (same shared `Suggestion`/`GapSuggestion` type).

**5. Fake Career Insights data.** `app/(protected)/resume/page.tsx` hardcoded the entire right rail regardless of the signed-in user — a duplicate, unused `career-insights-panel.tsx` component had the same hardcoded content and was never even imported (deleted). Replaced with `features/resume-toolkit/services/career-insights.ts`: top skills from the user's most recent saved resume, falling back to the most recent Optimiser run's uploaded resume when no saved resume exists; a real score-history sparkline from `resume_ats_reports` + `resume_optimizations`, with an honest empty/single-point state instead of a fabricated trend when there isn't enough history; and a real "matching opportunities" count via a single joined query against `opportunities`/`opportunity_tags` (an earlier version fetched matching tag rows and passed 400+ opportunity ids into a second query's `.in()` filter, which blew past PostgREST's ~16KB header limit and silently returned null for exactly the students whose skills matched the most listings — fixed to one joined `!inner` query with the tag filter server-side).

**Also added:** an inline resume preview (`resume-preview.tsx`) — Resume A/B now render as an actual formatted resume glimpse directly on the results card (name, summary, top skills) with a "Preview" button opening a full-page formatted view and a "Download" button, rather than only a bare score-and-download row. Hit the same `max-w-xl` → tiny-pixel collision (UI-03) as three earlier fixes this session; same inline-style workaround applied.

**Evidence:** every fix above was reproduced and re-verified live against the real production database, the user's actual account and actual resume/JD data (`resume_optimizations` row `7aa37efb-5e2b-42dc-af3a-5eb6de4ca92b`), not fixtures — see the JD-extraction consistency re-run (delta 0 after fix) and the Cloudflare-provider generation re-run (clean, zero fabrication findings) described above. Test-only artifacts (temp fixture files, temp test files hitting the real DB) deleted after use, no production rows created. 345/345 tests pass, TypeScript unchanged (53-line baseline incl. pre-existing untracked debug scripts, zero new errors from touched files), lint clean on all new files, production build passes. Visually verified in-browser: Career Insights rail (real data glimpse), resume mini-preview + full-preview modal (open, Escape-to-close, formatted layout).

**Not done — flagged, not fixed:** UI-03 (`max-w-*` Tailwind collision) now hit a 4th time; still only worked around locally each time, not fixed at the root in `globals.css` — deferred again given the blast-radius risk of renaming the underlying `--spacing-*` tokens without a full regression sweep.

---

### APP-08 · Certifications feature completed and populated with real data — **owner-requested, 11 Aug 2026**
**Found:** the `/certifications` page, its UI (`certifications-client.tsx`) and DB table already existed from earlier work, fully built and wired — but the ingestion pipeline that was supposed to feed it (`lib/certifications/ingest.ts`, Coursera + freeCodeCamp) was never actually connected to anything: no cron route, no entry in `vercel.json`, nothing ever called `collectCertifications()`. The table had 0 rows. The owner asked for Simplilearn, Udemy and Coursera specifically, "as much as possible," free and paid, with real logo/description/link/duration — no fake data.

**Sourced, live-verified, no fabrication:**
- **Coursera** (existing, now wired) — 3,000 real courses via the public catalogue API.
- **Microsoft Learn** (new) — official public Catalog API (`learn.microsoft.com/api/catalog`), no key required: 151 real paid-exam certifications (`has_certificate: true`) and 856 real free self-paced learning paths (`has_certificate: false` — a learning path is prep material, not the credential itself; marking it otherwise would overstate what completing it earns). Learning paths carry real `duration_in_minutes`; certifications don't (an exam has no runtime, left null rather than guessed).
- **Simplilearn** (new) — course pages are a client-rendered SPA with no server-side hydration blob (`__NEXT_DATA__` etc. all absent, verified live), so discovered 357 real course URLs via the site's own public sitemap (`sitemaps/www.sitemap_production.xml`, robots.txt explicitly permits these paths) and read each page's static schema.org `Course` JSON-LD for real title/provider/price — 148 successfully parsed. Duration is genuinely not recoverable this way and is left null rather than fabricated; the per-course "description" on the page is boilerplate (usually just the title repeated) so an honest generic line is used instead of presenting that boilerplate as real content.
- **freeCodeCamp** (existing) — 15 hardcoded, individually-verified free certifications.
- **Udemy** — re-verified genuinely blocked: course pages, robots.txt, and the sitemap all return 403 (WAF/bot protection), consistent with the prior documented finding. Not approximated or faked.

**Bugs found and fixed during this pass, all live-verified:**
1. **PostgREST's default 1000-row cap silently truncated the catalogue to 1,000 of 4,170 rows** — `.limit(2000)` in `app/(protected)/certifications/page.tsx` doesn't override the server's configured `db-max-rows`; a client-side `.limit()` above that ceiling is just ignored. Fixed by paging through with `.range()` until exhausted.
2. **2,639 of 4,170 rows (63%) had a `provider_logo` URL that failed to load** — Coursera's partner API returns a real number of logo URLs as plain `http://`, and the app's CSP (`img-src ... https:`) silently blocks the load rather than upgrading it, so most Coursera-sourced provider logos rendered as blank boxes. Found via the browser console's CSP violation log during visual verification, not by inspection. Fixed going forward in `ingest.ts` (`toHttps()`, applied to every source's logo field) and retroactively for the already-ingested rows via a direct `UPDATE ... SET provider_logo = REPLACE(...)`.
3. Simplilearn price values were sometimes numeric-looking strings ("17135.0000") rather than JSON numbers — coerced and trailing zeros stripped.

**Shipped:** `/api/cron/refresh-certifications` (weekly, Sundays 04:00 UTC, added to `vercel.json`), `lib/certifications/upsert.ts` (batched upsert on the table's `(source, source_id)` unique constraint — a re-run refreshes existing rows rather than duplicating them; certifications are never deleted on a schedule, matching the table's own "no reconciliation" design). Ran the full pipeline once manually against production: **4,170 real certifications** now live, zero upsert errors.

**Evidence:** every source was proven live before being trusted — Udemy's block re-confirmed via direct `curl` (403 on course page, robots.txt, and sitemap), Simplilearn's JSON-LD parsed from a real fetched page before writing the provider, Microsoft Learn's API response shape inspected directly. 4 real enrol/course URLs spot-checked post-ingestion, all HTTP 200. UI verified in-browser against the real 4,170-row table (not a fixture) via a temporary preview route reusing the actual `CertificationsClient` component: search and Free/Paid filtering confirmed working (an initial false alarm — reading DOM state synchronously in the same script tick as the triggering event, before React's render flushed — was ruled out by re-checking after a render tick), the detail modal opens with real duration/price/description and a working "Enrol now" link, and provider logos render correctly post-fix (verified via live `<img>` src inspection, not just visually). 345/345 tests pass, TypeScript and lint unchanged from baseline on all touched/new files, production build passes including the new cron route. Temp preview route and test files deleted after verification.

**Follow-up expansion, same day:** owner asked for English-only content, real certificate/badge preview images, "as many genuine providers as possible, even 50," and at least 10,000 certifications, plus a left filters sidebar (level, duration, more professional layout) matching the Search page.

- **English-only.** Coursera's API has a real `primaryLanguages` field (not previously requested) — added it to the fetch and filtered to `en`. Verified live: two independent 50-course pulls returned zero non-Latin-script titles after the change, vs. visible Japanese/Korean/Spanish titles in the owner's screenshot before it. Deleted and re-ingested all prior Coursera rows so no untagged non-English leftovers from the previous run could survive under the new, larger fetch.
- **Real certificate/badge images.** Added a `certificate_image` column (migration `20260811040000`), separate from `provider_logo` (the provider's brand mark, shown on every one of their listings) — populated only where a source genuinely provides a per-certification image, never a generic stand-in: Microsoft Learn's own real badge SVGs (1,007 records) and W3Schools' real certificate artwork (10 records, from each exam page's own `Product` schema `image` field). Left null everywhere else rather than reusing the course thumbnail or provider logo as a fake substitute.
- **4 new genuine providers**, each re-verified live before being trusted, not assumed from memory:
  - **edX** — previously assumed blocked (401 on the search/catalog API), but individual course pages carry real schema.org `Course` JSON-LD (`inLanguage`, `educationalLevel`, real `offers`, ISO-8601 `timeRequired`) and are reachable via edX's own sitemap. Language filtered on the page's real `inLanguage` field, not the URL — the sitemap mixes untranslated non-English course titles into un-prefixed paths, so a URL-only filter would have let them through.
  - **Udacity** — the `/catalog` page embeds its entire real course list as one schema.org `ItemList` directly in the page; no per-course crawl needed. Small (24 programs) but genuinely Udacity's whole flagship catalogue, not a partial scrape.
  - **W3Schools** — a fixed, known set of 12 real certification exam pages (mirrors freeCodeCamp's "hardcoded because it IS the catalogue" approach), each with a real schema.org `Product` block carrying genuine price, description and certificate artwork.
  - Re-probed and confirmed still genuinely unreachable without credentials: LinkedIn Learning, Pluralsight, Google Cloud Skills Boost, IBM SkillsBuild, Class Central. Not approximated or faked to inflate the provider count.
  - On "even 50 providers": rather than build 50 one-off scrapers of wildly varying reliability, the honest higher-leverage move was surfacing Coursera's own real partner network — the `provider` field was already set to each partner's own name (Google, IBM, Duke, Yale, Meta, hundreds of universities), not folded into a generic "Coursera" label. Counted directly post-ingestion: **395 distinct real provider names** across the 7 direct platform integrations.
- **Volume.** Raised the Coursera cap from 3,000 to 12,000 (real total catalogue is 23,422 courses per its own API). Ran the full pipeline: **13,369 fetched, 13,369 upserted, 0 errors** (freeCodeCamp 15, Coursera 12,000, MS certs 151, MS paths 856, Simplilearn 139, edX 174, Udacity 24, W3Schools 10). **13,378 total rows now live** (a handful of untouched rows from the prior Simplilearn run persisted alongside the fresh fetch).
- **Filters sidebar + level/duration.** Rebuilt `certifications-client.tsx` with a left sidebar mirroring the Search page's `FiltersSidebar` (static on desktop, slide-in drawer on mobile, matching `FilterSection`/checkbox/radio styling): Price, Level (Beginner/Intermediate/Advanced — the exact three values actually present in the data, checked live rather than guessed), and Duration in 4 buckets estimated from each record's own free-text duration string (the estimate is only ever used for bucketing — the original honest text, e.g. "6 weeks", "2.5 hours", is what's shown to the student, never the computed number). The detail modal now shows the real certificate/badge image when present, in its own "Certificate preview" section.

**Evidence (follow-up):** every new provider's real data structure was inspected directly via `curl`/Python before any ingestion code was written — edX's `Course` JSON-LD fields, Udacity's `ItemList`, W3Schools' `Product` schema with a live price ($95) and real certificate PNG. Live-verified in-browser against the real 13,378-row table: Advanced-level filter → 101 real Microsoft Learn results with real per-course durations; W3Schools search → 10 real exam listings; opened both a Microsoft Learn certification (real hexagonal achievement badge rendered in the modal) and a W3Schools exam (real "HTML5 · Certified HTML Developer" badge artwork, real $95 price) to confirm the certificate-preview feature end to end, not just that the field was populated. Zero `http://`-scheme image URLs anywhere in `provider_logo` or `certificate_image` post-run (checked directly against the DB). 345/345 tests pass, TypeScript and lint unchanged from baseline, production build passes with both the certifications page and cron route present. Temp test/preview files deleted after use.

---

### SEC-01 · AI rate limiting covered 4 of 17 features — **HIGH**
**Found:** `RATE_LIMITS` in `lib/ai-gateway/index.ts` defined only `resume_parser`, `resume_ats`, `resume_optimizer`, `skill_extraction` (plus `resume_polish`/`resume_target` added in APP-02). `checkRateLimit` did `if (!limit) return true`, so every other declared `AIFeature` — `jd_intelligence`, `evidence_evaluation`, `resume_ats_v2_*`, `hr_coaching`, `assistant`, `schema_repair`, `resume_extraction`, `resume_optimization`, `resume_ats_jd_extract`, `resume_ats_coaching`, `resume_ats_general_coaching` — was completely unlimited. An authenticated user could drain the provider quota through any of them.
**Fixed:** 10 Aug 2026. Added a `DEFAULT_LIMIT` (30/hour) fallback in `checkRateLimit`, applied to any feature without its own entry, replacing the `return true`. Per-feature tuned limits should still be added as real usage patterns become known — this is a backstop, not a claim that 30/hour is the right number for every feature (e.g. `assistant` chat likely wants its own, higher limit).
**Evidence:** new test `tests/ai-gateway-rate-limit-default.test.ts` — mocks the provider chain and the Supabase RPC backstop, then calls `callAI` with `feature: 'assistant'` (a real, previously-unlimited `AIFeature`) 30 times successfully and asserts the 31st fails with `reason: 'rate_limit'`; a second test confirms anonymous (no `userId`) calls remain unthrottled by design. Suite: **279/279**. TypeScript unchanged at 36. Lint clean on both touched files — the 2 pre-existing `no-explicit-any` errors in `lib/ai-gateway/index.ts` are unrelated lines untouched by this change (confirmed via `git stash` diff before/after).

---

### DATA-01 · 376 listings link to a search page, not the job — **the audit's number was wrong; 170 actually needed fixing**
**Found:** the original audit flagged 376 rows by URL *pattern* (`/search`, `/open-positions`, `/careers-list` in the URL) as dead-end links, without clicking through per company.

**Re-investigated:** 10 Aug 2026, with the owner present. Clicked through real sample URLs (2–3 per company, in a live browser, JS executed) for every company on the list, not just the URL shape:

| Company | Rows | Result |
|---|---|---|
| Stripe | 126 | **Broken** — every sample landed on an unfiltered 549-job list; the `gh_jid` query param is not consumed client-side |
| HighRadius | 44 | **Broken** — every sample landed on the marketing homepage, not even a job list |
| Databricks | 125 | **Works** — every sample deep-linked to the correct job title and description |
| MongoDB | 52 | **Works** — same |
| Fivetran | 29 | **Works** — same |

Only **170** of the 376 (Stripe + HighRadius) were real dead-ends. The other 206 were a false positive from the pattern-matching heuristic — their embedded Greenhouse widgets do correctly resolve `gh_jid` client-side, the URL shape just looked generic.

For the 170 real dead-ends, checked whether the audit's suggested fix (`https://job-boards.greenhouse.io/<slug>/jobs/<id>`) provides a working alternative: it does not — for both Stripe and HighRadius, every Greenhouse-hosted-domain variant (`job-boards.greenhouse.io` and `boards.greenhouse.io`) 301-redirects to the same broken company page. Both companies have fully migrated to a custom-domain embed with no working per-job deep link anywhere. There is no honest URL to reconstruct.

**Fixed:** 10 Aug 2026.
```sql
UPDATE opportunities SET status = 'Expired', updated_at = NOW()
WHERE company_name IN ('Stripe', 'HighRadius') AND status = 'Published';
```
This is a data-only change — no schema modification, so the full schema guard wasn't the relevant check. `opportunity-service.ts` and the `search_opportunities_rpc` fast path both filter `status IN ('Published', 'Closing Soon')`, so `Expired` rows are excluded from every search path a student can reach.

**Evidence:** dry-run `SELECT ... GROUP BY company_name, status` before the update showed exactly 170 rows, all `Published`. Re-ran the same query after the `UPDATE` with a real row-select (not `head:true`) — confirmed **170/170 now `Expired`**, split 126 Stripe / 44 HighRadius as expected. Databricks/MongoDB/Fivetran rows were not touched.

---

### DATA-02 · Internshala is 73% dead — **audit's number held up under re-verification**
**Found:** the audit reported 27 of 37 Internshala listings rendering *"Applications are closed for this internship."*

**Re-verified:** 10 Aug 2026. Pulled all 32 `Published` Internshala rows (5 of the 37 were already `Expired`) and fetched each `apply_url` directly (not head:true, not a pattern match — the literal marker text) with a real browser user agent. **22 of 32 showed the closed banner; 10 were genuinely open.** 22 + the 5 already-Expired = 27 — matches the audit exactly. Spot-checked one closed and one open result in an actual browser (not just curl) to rule out a client-side-rendered banner producing a false negative: both matched the curl-based read.

**Root cause:** Internshala returns HTTP 200 with the closed banner embedded in the page rather than 404ing — nothing in the ingestion pipeline ever read the page to notice, so a closed posting kept getting re-confirmed as live (`last_seen_at` refreshed) on every run indefinitely.

**Fixed:** 10 Aug 2026, two parts.
1. **Data:** `UPDATE opportunities SET status = 'Expired', updated_at = NOW() WHERE id IN (the 22 confirmed IDs) AND status = 'Published'`. Verified before (22/22 `Published`) and after (27 `Expired` / 10 `Published` for the full Internshala set, matching 5 already-expired + 22 newly-expired) with real row-selects.
2. **Scraper:** added `src/providers/opportunities/utils/internshalaClosedDetector.ts` (`isInternshipClosed()`), wired into both `InternshalaProvider.fetchDetailPage()` (throws, so the queue-consumer path marks it failed rather than upserting it) and the detail-enrichment loop inside `fetch()` (the path actually running in production via `/api/cron/refresh-internshala` — excludes closed items from the batch entirely, so they stop being refreshed as live and fall through to the existing reconciliation gate instead).

**Evidence:** new tests — `tests/internshala-closed-detector.test.ts` (4 cases: real captured banner text, case-insensitivity, an open posting, and a false-positive guard against unrelated "closed" mentions) and `tests/internshala-provider-closed.test.ts` (`fetchDetailPage` rejects a closed posting, returns normally for an open one, mocking `fetch` directly). The `fetch()` end-to-end path itself isn't separately tested — its list-scraping logic loops over 18 category URLs × pagination and mocking that fully wasn't worth the cost — but the detection logic both paths call is fully unit-tested and the wiring at each call site is a small, reviewed diff. Suite: **285/285**. TypeScript unchanged at 36. Lint: the 9 errors/6 warnings in `InternshalaProvider.ts` are pre-existing (confirmed via `git stash` diff, same 15 problems before and after); the new files are clean.

---

### DATA-03 · Link verification never ran — **scoped deliberately narrower than "detect any dead link"**
**Found:** `link_status`/`link_checked_at` have existed since the trust-engine migration; nothing ever populated them — `NULL` on all 4,175 live rows.

**Scope decision, made explicit because DATA-01 is a cautionary tale here:** DATA-01 showed that HTTP status code alone can't tell a real per-job page from a broken one — Stripe and Databricks both return 200 for their `?gh_jid=` URLs, and only reading the rendered content told them apart. A status-code sweep can't replicate that per-company judgement call at 4,000-row scale. So this sweep does exactly what its name says — verifies the HTTP-level outcome — and only auto-expires the subset where that's unambiguous regardless of page content: the URL doesn't resolve at all (DNS/connect failure), or the host itself says the resource is gone (404/410). A 403/401/405/5xx is recorded but left alone, because the original audit's own Amazon false-positive (a "404 error" string inside analytics JS) is exactly the failure mode of trusting an ambiguous signal.

**Built:** 10 Aug 2026.
- `lib/ingestion/link-checker.ts` — `checkUrl()` (HEAD, falling back to GET on 405/501 or a thrown error; resolves to `status: 0` rather than throwing, so one bad host never stalls a batch) and `sweepLinkHealth()` (selects live rows ordered `link_checked_at NULLS FIRST`, checks at bounded concurrency within a time budget, writes `link_status`/`link_checked_at` for every row checked, and auto-expires only the unambiguously-dead ones).
- `app/api/cron/link-sweep/route.ts` — same fail-closed `denyIfNotCron` pattern as every other cron, `maxDuration = 300`, a 270s internal time budget so the function always returns before Vercel's own ceiling. Scheduled nightly at `0 2 * * *` in `vercel.json`, after the other ingestion crons.
- Resumable by construction: a time-budget cutoff mid-sweep is safe — everything checked so far is already written, and the next run picks up the least-recently-checked rows first.

**Evidence:**
- 9 new tests in `tests/link-checker.test.ts`: `classifyLinkStatus` (0/404/410 dead, 403/401/500/301 not), `checkUrl`'s HEAD→GET fallback on both a 405 and a thrown error, and `sweepLinkHealth` against a fake Supabase-shaped `Db` — confirms per-row `link_status` writes, that only the 404 in a 3-row mixed batch gets expired (403 does not), and that the time-budget cutoff actually stops the loop rather than overrunning. Suite: **294/294**. TypeScript unchanged at 36. Lint clean.
- **Live proof-run against production**, not just mocks: ran `sweepLinkHealth` directly (via `tsx`, real service-role client, `limit: 12`) against the actual database. Result: `{ checked: 12, ok: 12, dead: 0, expired: 0 }`. Verified independently with a real row-select (not `head:true`) — `SELECT count(*) WHERE link_checked_at IS NOT NULL` → **12**, all `link_status = 200`. The scratch script was not committed.
- The full nightly sweep has not yet run in production — it starts at `0 2 * * *` once this deploys. The 12-row proof-run above confirms the mechanism works end-to-end; it does not confirm throughput at the full ~4,000-row scale within the 300s ceiling. Worth checking after the first scheduled run.

---

### UI-03 · Tailwind theme-collision — fixed at the root, not worked around a 5th time — **12 Aug 2026**
**Found (again):** a real user report — the Certifications page's empty-state text was rendering one word per line. This is the same bug already hit and locally worked around 4 separate times (Resume Optimiser, Hub profile modal ×2, Hub error state) without ever being fixed at the source, each time deferred as too risky to fix without "a full regression sweep of every component using `max-w-{sm,md,xl}`."

**Root cause, confirmed against Tailwind v4's actual compiled CSS output, not assumed:** `--spacing-{xs,sm,md,lg,xl}` in `globals.css` (4/8/16/24/40px, added for `p-md`/`gap-md`-style utilities) are hard-wired into `max-width: var(--spacing-<key>)` for `max-w-{xs,sm,md,lg,xl}` specifically — unlike `max-w-2xl` and up, which read a separate, uncontested `--container-*` scale. So these five keys were never actually free to reuse; whatever value they held became the max-width for those classes project-wide, silently, everywhere, not just where someone had noticed and patched it locally.

**Fixed:** the full regression sweep the prior 4 attempts kept deferring. Mechanically rewrote all 234 call sites using `p-xs`/`gap-md`/`mb-lg`/etc. across 15 files to Tailwind's own numeric scale (`xs`→`1`, `sm`→`2`, `md`→`4`, `lg`→`6`, `xl`→`10` — exact pixel matches for the old values), freeing the five keys up entirely. Redefined them to the values `max-w-{xs,sm,md,lg,xl}` are actually documented to have (20/24/28/32/36rem), fixing every occurrence in the app at once instead of a 5th local patch. Removed the 4 existing inline-style workarounds now that the real classes resolve correctly.

**Evidence:** verified directly against the compiled CSS output (not just a screenshot) — `max-w-xs` through `max-w-xl` now compute to 320/384/448/512/576px exactly; `max-w-2xl` (the always-worked control) unchanged at 672px; `p-4`/`gap-4`/`mb-4` (numeric scale) unchanged at 16px. 345/345 tests, typecheck clean, production build clean. Committed `1413e73`, pushed.

---

### AUDIT-12AUG · Strict, live-verified audit of Certifications, AI Assistant, Voice Interview, AI Search, ATS Score, and Resume Optimisation — **owner-requested, 12 Aug 2026**

**Found and fixed, Certifications:**
1. **Duration filter regex missed common abbreviations.** `hrs`/`hr`/`h`/`hs` and hyphenated forms ("2-hour course") never matched — only spelled-out "hour(s)" did. Verified against all 20,634 production rows: 113 rows recovered, zero regressions.
2. **Pagination had no stable tiebreaker** past `is_free, title` — 269 rows (1.3%) share a sort key, so offset/limit could duplicate or skip rows at a page boundary. Added `id` as a final tiebreaker to the paginated search, the cached first page, and the provider-count query (which had no `ORDER BY` at all).
3. Live URL health sample (250 rows spread across the table): 250/250 healthy.

**Found and fixed, AI Assistant:**
4. **A live, unauthenticated endpoint** (`app/api/assistant/setup/route.ts`) used the service-role key with no auth check at all — a one-time table-setup helper never removed. Chat tables confirmed to already exist in production, so it currently only leaked "tables already exist," but if that condition ever flipped it would have handed any anonymous caller the full schema + a direct Supabase dashboard link. Deleted.
5. Masked type error (`useRef<HTMLElement>` on a `<div>` ref) — only invisible because `next.config.ts` sets `typescript.ignoreBuildErrors: true` project-wide (see CODE-01). Fixed the one instance; the config flag itself is unchanged, a separate decision.
6. **Flagged, not fixed — needs an owner decision:** `regenerateLastResponse` deletes the previous AI answer before confirming the replacement generated successfully. A failure between those two steps (AI gateway error, DB write failure) permanently loses a working answer, and the fallback error text shown is never persisted, so it's gone on reload too. Left for the owner to choose the right recovery UX rather than guessing.

**Found and fixed, AI Voice Interview (code-level only — still nothing deployed):**
7. **A real database race**, caught before the migration ever ran in production: `interview_reports` had no uniqueness constraint on `session_id`, and the status route wrote to it with a plain `INSERT` on every poll. The browser's disconnect handler and the server-side polling fallback can both observe "just completed" before either write commits, inserting two rows for one session — which then makes the terminal-status read path's `.maybeSingle()` error on multiple rows, permanently hiding an already-completed interview's report. Added a `UNIQUE` constraint and switched the write to `upsert`.
8. **A frozen-UI bug**: `fetchStatus` only handled non-2xx HTTP responses, not network-level failures — one dropped request during the initial load or the post-interview scoring poll threw unhandled, silently freezing the page on a loading/scoring spinner forever with no retry and no timeout ever firing. Fixed both call sites to retry on a genuine network failure instead of dying silently.

**AI Search:** code-level audit, came back clean — no bugs found. Ownership checks correct on every route, job-status writes are idempotent updates (not the insert pattern that caused the interview race above), upload validation matches the agent's own checks.

**ATS Score + Resume Optimizer — the first genuinely live-verified pass on either feature**, using a real authenticated session (the owner's own sign-in), closing the gap every prior pass on these two features explicitly flagged as the one thing only the owner could do:
9. **The resume picker showed a raw database UUID** instead of the resume's title, in both features' selectors, after selection. Root cause: Base UI's `SelectValue` has no Radix-style automatic label lookup — it needs an explicit `children` render function (its own docs say so) or it falls back to printing the raw selected value. Neither selector provided one.
10. **`/resume` — the shared entry point to both features — had zero responsive breakpoints.** Two fixed 280px sidebars left the center column at 159px wide on a normal 831px browser window, breaking every heading into single words. Added a stacking breakpoint at `lg` (1024px).
11. **The ATS Checker's "why are these fields disabled" confusion** — Target Role/Company are deliberately disabled until a JD is entered (the scoring engine genuinely can't target-score without one), but the form never explained why, so it read as broken. Added a visible explanation line. Re-verified with real clicks and typing (not script-injected values) that the disabled state is genuine and the fields enable correctly once a JD is present.
12. Two full live runs proved the actual pipelines work end to end on a real account: ATS Check scored a near-empty resume 2/Poor (honest, all-critical-gaps) and a realistic resume 56/Partial with real evidence citations quoting the resume's own text; the Optimizer generated Resume A, honestly re-scored it (barely moved, since there was genuinely nothing to polish), and a downloaded PDF was verified byte-for-byte (`%PDF-1.3` magic bytes, correct filename, real content).

**Built, not a bug fix — multi-key-per-provider rotation**, owner-requested for demo-day resilience: Gemini, Groq, OpenRouter and Ollama can each hold several API keys via numbered env vars; a rate limit or auth failure on one key falls through to the next key of the *same* provider before the gateway ever tries a weaker provider. Covers AI Assistant, ATS Score and Resume Optimizer (all three route through this shared gateway) — does not and cannot cover AI Search or Voice Interview, which are separate, undeployed services with their own provider config. Found and fixed a real bug while wiring it up: `gemini-2.5-flash` (a pinned version) 404s as "no longer available to new users" on newer Google Cloud projects while still serving older ones — switched to `gemini-flash-latest`, Google's stable alias, which works across both. Added Mistral as a new provider at the owner's explicit request, after flagging that its free tier trains on submitted requests — a real tradeoff for a feature handling student resumes and job descriptions, confirmed accepted rather than assumed.

**Evidence:** live-verified end to end, not mocked — a real `callAI()` run hit `auth_failure` on Gemini key 1, empty responses on keys 2–3, and succeeded on key 4, with the caller getting a clean response regardless. 14 new tests for the key-pool/rotation logic. 359/359 tests total, typecheck clean, production build clean. Two commits, both pushed: `f9d08f0` (audit fixes) and `b271881` (gateway rotation).

**Not fixed, flagged for the owner:** item 6 above (regenerate data-loss). **Not deployed, unaffected by anything in this pass:** AI Search agent, DeepInterview voice agent — both still need Oracle Cloud VM provisioning before either can be demoed.

---

### AUDIT-16AUG · Full live user-experience audit of Resume Optimiser/ATS, AI Search, and Deep Interview, plus a wider app pass — **owner-requested, 16 Aug 2026**

**Scope:** a fresh test account was created (not one of the five real `auth.users` accounts — signed up and confirmed via the service-role admin API, same as any real student would after email verification), then used to live-test the app end to end: Resume Builder → ATS Checker → Resume Optimiser (full run, checklist, both PDFs downloaded and byte-verified), Search filters, Hub, Certifications, AI Assistant, session login/logout. In parallel, two independent code-level audits covered the AI Search agent (`/Users/laxmansirvi/ai  agent `) and the DeepInterview voice stack (`/Users/laxmansirvi/DeepInterview`) plus their Opportunity Radar integration code, since neither is deployed anywhere reachable and both needed their actual current state re-established — the last real numbers for either (AI Search 50%, Voice Interview 2%) were from 12 Aug and, for interview, from before it had any Opportunity Radar-side code at all.

**9 real, live-reproduced bugs found and fixed, all re-verified after the fix:**

1. **`/signup` rendered the Login form, not Signup** — `AuthExperience` is shared between `/login` and `/signup` but hardcoded `activeTab` to `'login'` regardless of which route rendered it. A student following a "Sign up" link from anywhere would land on a page titled "Sign Up" showing the Log In form. Fixed: `activeTab` now derives from `usePathname()`. *(AUTH-01)*
2. **Every new signup's display name silently became their email's local-part**, discarding the "Full Name" field entirely. Root cause: `public.handle_new_user()` (the `auth.users` insert trigger) reads `raw_user_meta_data->>'name'`, but `signupAction` has only ever sent the field as `full_name`. Reproduced live: a fresh signup with Full Name "Audit Tester" produced `profiles.name = 'laxmansirvi2203+audit20260816'` — confirmed by reading `auth.users.raw_user_meta_data` directly against the resulting `profiles` row. This affects every student who has ever signed up through the form. Fixed two ways: `signupAction` now sends the name under both `full_name` and `name` (takes effect immediately, no deploy of the DB change needed), and migration `20260816090000_fix_new_user_name_metadata_key.sql` makes the trigger itself check `full_name` first — **not yet applied to production**, needs the owner's normal migration-apply step. Existing users' `profiles.name` was **not** mass-corrected — a student may have already fixed their own name via Settings since signing up, and there's no way to tell that apart from an unedited default. *(AUTH-02)*
3. **UI-01 fixed**: the `<br>` between "opportunity" and "is" in the login/signup headline had no accompanying whitespace, so browsers under 1024px rendered "opportunityis" as one word. Added a literal space alongside the conditional `<br>`.
4. **UI-02 fixed**: the login form sat below the fold on any viewport under ~1100px tall, because the branding/illustration section rendered above the form in the mobile-stacked layout. Added `order-last lg:order-none` to the branding section so the form renders first below `lg`, restoring the original left/right split at desktop width. Both re-verified visually: headline reads correctly, login form is the first thing visible on a 768×1024 viewport.
5. **138 published listings simultaneously showed "Onsite" and "Remote"** on the same search-result card. Root cause: `UnstopProvider.normalize()` computes `mode` from `jobDetail.type` and `location` from `region` — two independent fields in Unstop's API that can disagree. Whenever `region` was `'online'` or blank (common, and unrelated to work mode) the code defaulted `location` to the literal string `'Remote'` regardless of what `mode` said, producing self-contradictory listings. Confirmed via direct query: all 110 `mode='Onsite'` + `location='Remote'` rows and all 28 `mode='Hybrid'` + `location='Remote'` rows were `source='unstop'`, zero from any other provider. Fixed the ingestion code (only defaults `location` to `'Remote'` when `mode` agrees; otherwise `'Location TBD'`, matching the search card's own existing fallback text) and corrected all 138 already-published rows in production directly, re-verified with a real row-select (0 remaining contradictions) and live in the UI (filtering Onsite now shows "Location TBD", not "Remote"). *(DATA-08)*
6. **The entire 20,596-row Certifications catalogue could render as "0 certifications found"** — indistinguishable from a genuinely empty catalogue — whenever the first-page database query hit a transient timeout. Caught live: `[Certifications] first-page query failed: canceling statement due to statement timeout` in the server log, immediately followed by the page showing "No certifications found" while its own sidebar still listed real per-provider counts (Coursera 1,365, etc.) — a direct contradiction that gave away the failure was being swallowed. Root cause: `fetchFirstPage()` caught the DB error and returned `{ items: [], total: 0 }` — a normal-looking success value — which `unstable_cache` then memoized as fact for 900 seconds; a single slow query moment would have shown every student an empty catalogue for the next 15 minutes. Fixed: the function now throws instead of swallowing the error (so `unstable_cache` never memoizes a failure), the page catches the throw and passes a `loadError` flag, and the client renders a distinct "Couldn't load certifications — try refreshing" state instead of the "No certifications found" empty state. Re-verified: the identical query now succeeds in ~1s returning the real 20,596-row count; the honest failure state was confirmed by temporarily forcing the throw. *(APP-09)*
7. **A real hydration mismatch on the AI Assistant's send button**, logged by React on every page load whenever a draft existed from a previous visit (`InputArea`'s `useState(() => localStorage.getItem(DRAFT_KEY) ?? "")` read `window`/`localStorage` inside the state initializer, which runs during the client's first render too — before hydration reconciles against the server's forced-empty string). React explicitly logs "this won't be patched up." Fixed: state now always starts empty (matching SSR), and the saved draft loads in a `useEffect` after mount instead — re-verified with a clean console (no warning) on first load, and the draft still restores correctly on a subsequent load. *(APP-10)*
8. **The AI Assistant fabricated "Here are some opportunities that match your criteria" with nothing behind it.** Reproduced live with the query "What internships match a frontend developer skilled in React?" — the assistant's own opportunity-search trigger (`isOpportunityQuery`) only recognised `find/search/show/list/look/recommend/available/open` as search verbs; "match" wasn't in the list, so the real database search never ran at all. But the model, seeing an obvious opportunity-search question, said the "here are some opportunities" line anyway and then listed none — a genuine instance of exactly the fabrication class this project's own docs treat as a first-order sin everywhere else (the Resume Optimiser's fabrication guard, the AI Search/Voice Interview integration contracts). Fixed both layers: widened the trigger regex to include `match(es)/matching/suit(s)/suited/fit(s)`, and added an explicit system-prompt instruction forbidding the "here are opportunities" framing unless the prompt confirms results were actually attached — including a distinct branch for "searched, found zero" so a real empty search reads honestly rather than being silently indistinguishable from "never searched." Re-verified with the identical query: the assistant now correctly says "I searched our database, but there are currently no frontend internships specifically matching React available right now" and suggests broadening the search. *(APP-11)*
9. **`POST /api/interview/start` had no rate limiting of any kind** — every other AI-backed route in the app throttles through `callAI`'s shared rate limiter, but the interview-start route calls the separate DeepInterview service directly and never routed through the gateway at all, so it inherited none of that protection. An authenticated student could spam session creation, each call triggering a real external session-prep request with its own provider cost. Fixed by exporting `checkRateLimit` from `lib/ai-gateway` and adding a `voice_interview: { max: 10, windowMs: 86_400_000 }` entry, checked before any work begins in the route. *(SEC-04)*

**Also confirmed working, no changes needed** — Resume Builder autosave (verified by reading the raw `parsed_data` JSON back from the database after typing), the Builder's Zod schema correctly rejecting malformed section data rather than silently corrupting a resume, the ATS Checker's evidence-based scoring (real citations quoting the test resume's own text, correct partial/complete distinctions), the Resume Optimiser's full pipeline (baseline → Resume A → checklist → Resume B, both variants downloaded as real PDFs — `%PDF-` magic bytes, correct filenames, 4.4–4.6KB each), the fabrication guard live-catching and retrying an over-claiming Resume B generation attempt (`[Optimizer] attempt 1 rejected — section_growth: ...`), the AI Gateway's multi-key failover (watched live: Gemini key 1 → 403, key 2/3 → 503, key 4 → success, all inside one request), the resume-picker showing titles not UUIDs, Hub's sender-name/avatar resolution and image rendering, Search's filter sidebar and result counts, and session login/logout (auth cookie correctly cleared, protected routes correctly redirect to `/login` afterward).

**One finding investigated but not fixed — a real, unexplained scoring swing, documented rather than guessed at:** the same underlying resume content scored 70 (baseline) → 68 (Resume A, reworded but factually identical) → 57 (Resume B, after confirming all 4 checklist items — but the fabrication guard correctly refused to add unproven claims, so Resume B's actual content is nearly identical to Resume A's). Evidence evaluation already runs at `temperature: 0.0` (a prior fix, confirmed still in place), so this isn't sampling noise — it's the evidence-matching LLM call giving a meaningfully different judgement (13 points) on two near-identical texts across independent calls. Not fixed because the right fix isn't obvious without a deeper diff of exactly which requirement's `satisfaction` flipped between the two evaluations — flagged for a follow-up pass rather than patched speculatively.

**Also flagged in this pass, both fixed later the same day — see AUDIT-16AUG-SEC below:** the `/api/assistant` route validating the request body before checking auth (SEC-05), and CSP's `unsafe-eval` (SEC-02).

**AI Search — state corrected from a stale "50%, agent unreachable" to a fuller picture, independent code audit:** the Opportunity Radar side remains complete and honest (agent-unreachable surfaces as a real error, not an infinite spinner; a 30-minute poll ceiling force-fails a genuinely stuck run). New since 12 Aug: real Oracle Cloud deployment tooling now exists in the agent repo (`deploy/oracle-setup.sh`, systemd units, a Caddy reverse-proxy config implementing the shared-secret gate the handoff doc asked for) — but none of it has been run. `AI_AGENT_URL` is still `http://localhost:4300`, confirmed unreachable right now. The shared-secret header the frontend sends (commit `0f4d142`) has nothing on the agent side that checks for it yet — enforcement is designed to live at the Caddy layer, which doesn't exist yet either. The agent's own architecture is simpler than the original handoff doc implied: n8n runs as a one-shot CLI subprocess per job, not a persistent service. Local Postgres/Redis are already running on this machine with the schema applied and real provider API keys already sitting in the service `.env` files — the fastest next step is proving the pipeline still runs locally (create the one missing `job-server/.env`, `npm start` each service) before spending time on VM provisioning. Real ceiling once deployed: Groq's free tier supports roughly one student per day at ~68,800 tokens/run — a commercial limit, not an engineering one.

**Deep Interview — state corrected from a stale "2%, nothing built" to "~35–40%, extensively built but entirely unconfigured," independent code audit:** two commits since the 12 Aug audit (`db5f89c`, `e794a7e`) built a real, 1,586-line Opportunity Radar-side integration — correct auth on every route, real ownership-scoped queries, a genuine LiveKit WebRTC voice room (not the turn-based text/Web-Speech-API approach the original handoff doc recommended — a deliberate, disclosed, architecturally sound deviation since LiveKit audio never touches Vercel's 300s ceiling), a real mic-level pre-flight check, and a report view that omits missing fields rather than fabricating them. The `interview_sessions`/`interview_reports` migration exists locally with correct RLS and the `UNIQUE` constraint from the 12 Aug database-race fix, but **there is no evidence it has been applied to production**. Zero configuration exists anywhere: no `INTERVIEW_AGENT_URL`, `LIVEKIT_URL`, `LIVEKIT_API_KEY`/`SECRET`, or `INTERVIEW_AGENT_INTERNAL_SECRET` in any env file, so the token route always 503s and the feature cannot function even in demo form right now. Separately good news: the DeepInterview repo itself already moved off the heavy local Ollama/Whisper/Kokoro stack the 10 Aug doc worried about — its defaults are now hosted providers (Deepgram STT, Cartesia TTS, Gemini LLM), matching the doc's own "Option A, recommended" — but none of those provider accounts are provisioned either.

**Migration guidance for both features** — concrete env vars, deployment steps, and handoff prompts for finishing AI Search and Deep Interview (including the option of replacing either external service with a lighter, self-hosted approach) — written up separately for the owner, not duplicated here: see the audit report delivered alongside this update.

**Evidence for every fix above:** 405/405 tests passing (up from 359 on 12 Aug — resume-toolkit work between sessions added tests of its own), TypeScript unchanged at 35 (36 baseline, one file's error count shifted incidentally, no new errors introduced by anything in this pass — confirmed via `git diff` on every touched file), lint clean on every new/touched file, production build (`npm run build`) passes with all routes registering correctly including the three touched API routes. Every fix was re-verified live in a running dev server after the change, not just by reading the diff — the certifications fix against the real 20,596-row production table, the Unstop data fix against a live-filtered search result, the AI Assistant fix by resending the identical failing query and reading the new response.

---

### AUDIT-16AUG-SEC · Closing remaining open security/code-quality items — **owner-requested, same day**

**SEC-05 fixed:** `/api/assistant` now checks auth before validating the request body, matching every other route.

**SEC-02 fixed — CSP tightened without the risk of a full nonce rewrite.** Per Next.js's own CSP guide (`node_modules/next/dist/docs/01-app/02-guides/content-security-policy.md`): *"unsafe-eval is not required for production. Neither React nor Next.js use eval in production by default"* — it was set unconditionally in both dev and prod. Now dropped for production, kept for dev (React's own error-stack reconstruction needs it there). Added `'wasm-unsafe-eval'` in its place, since the Resume Builder's layout engine (`yoga-layout`) instantiates WebAssembly and needs its own, narrower grant. `'unsafe-inline'` was deliberately left alone — removing it requires a nonce-based rewrite that forces every route into dynamic rendering, a real architectural/caching tradeoff not attempted in this pass.

**Live-verified, not just read off the docs:** built a real production bundle (`npm run build`), ran it standalone (`next start`) on a clean port, confirmed the served header actually dropped `unsafe-eval` (`curl -I` on the live response), then exercised the single heaviest, highest-risk surface in the app — the Resume Builder (tiptap, `yoga-layout` WASM, `react-pdf`, `dnd-kit`) — including generating a real PDF client-side, plus Hub and the AI Assistant, watching the console for CSP violations the whole time. Zero violations anywhere. Reverted to the dev server afterward; the temporary prod instance was killed.

**SEC-03 fixed:** deleted `login-form.tsx`/`signup-form.tsx` (confirmed zero real importers — the one grep hit on `signup-form` was a DOM element `id`, not an import). Both read `?next=` with no validation; harmless while dead, a real open-redirect risk the moment anyone wired them up.

**CODE-03 fixed:** deleted `/api/opportunities/recommended` — confirmed zero callers (`grep` across the app) and confirmed the RPC it depends on, `get_ranked_opportunities`, does not exist (`PGRST202` when called directly against production). It would 500 unconditionally for anyone who ever called it.

**CODE-02 mostly fixed — the real story was three separate gaps stacked, not one.** "6 test files import `@testing-library/react`, which isn't installed" was accurate but incomplete:
1. The missing dependency (fixed: installed `@testing-library/react` + `@testing-library/jest-dom`, React-19-compatible versions).
2. `vitest.config.ts`'s `include` only ever matched `tests/**/*.test.ts` — these files live under `features/**` and `libs/**` as `.test.tsx` and were never collected at all, dependency or not (fixed: widened the glob).
3. `jest-dom`'s matchers (`toBeInTheDocument`, etc.) were never registered against Vitest's `expect` (fixed: added `vitest.setup.ts`).
4. A fourth gap surfaced only once the first three were fixed: `@reactive-resume/*` imports don't resolve under Vite at all — `node_modules/@reactive-resume/` is not actually a linked workspace (confirmed empty), and Next.js only resolves these via `tsconfig.json`'s `paths`, which Vite doesn't read. Fixed by mirroring those paths into `vitest.config.ts`'s `resolve.alias`.

**Net result: 2 of 7 previously-broken files (`base.test.tsx`, `gallery.test.tsx`†) now genuinely pass** — `base.test.tsx` recovered for real (4 new tests, 405→409). †`gallery.test.tsx` turned out to hit the same remaining blocker as the other 5 and is excluded alongside them: all six exercise code that calls Lingui's `t`/`msg` macro tags, which only work when compiled away by `babel-plugin-macros` at build time — Next.js gets this via `@lingui/swc-plugin`, but Vite/Vitest has no equivalent wired in. Installing `@vitejs/plugin-react` to add one hit a real peer-dependency conflict (React 19 vs. this project's Vite/Vitest version) that needs a deliberate dependency decision, not a silent `--legacy-peer-deps` override during an unrelated pass. The 5 (6 counting gallery) are excluded from the run with a comment explaining exactly why, rather than left to fail red for a cause unrelated to whatever someone else is actually working on.

**Evidence:** 409/409 tests passing (405 baseline this session + 4 recovered), 49/49 files (up from 48). TypeScript errors **dropped from 35 to 19** — the newly-collected test files were apparently also failing `tsc`'s project-wide check even while vitest silently skipped them; fixing their imports fixed that too. Lint clean on every touched file. Production build passes.

---

### AUDIT-16AUG-POLISH · Design/UX pass across Auth, AI Assistant, Search, Hub, Certifications — **owner-requested, same day, "push every rating toward 9.5"**

**The single highest-value finding of this entire audit, found while polishing the Search page:** `<Toaster/>` (sonner's toast renderer) was mounted in exactly one place in the whole app — inside the Resume Builder's own `page-client.tsx`. Every other page that calls `toast.success()/.error()/.loading()` — the ATS Checker, Resume Optimiser, AI Search, Tracker, notifications, resume upload/import dialogs, bookmarking — was calling into a toast queue with nothing anywhere in the tree to render it. **Confirmed live**, not inferred: submitted a real ATS check on `/resume/ats`, watched `toast.loading("Analyzing ATS Compatibility...")` fire correctly in the code path and produce literally nothing on screen — no popup, no corner notification, nothing — despite the analysis itself completing correctly moments later. Every loading/success/error notification on every one of those flows has likely never been visible to a single user.

**Fixed:** added `"use client"` to `packages/ui/src/components/sonner.tsx` (the themed Toaster wrapper — it used `useTheme()` but had no client boundary of its own, which only worked by accident while it was exclusively imported from an already-`"use client"` file), mounted it once in `app/layout.tsx` so every route gets it, and removed the now-redundant local one from the Resume Builder (two simultaneously-mounted Toasters would have double-rendered every toast on that specific page). **Re-verified live**: resubmitted the same ATS check — a real "✓ Analysis complete!" toast now renders bottom-right, confirmed via DOM inspection (`aria-label="Notifications alt+T"`, sonner's own signature), and confirmed exactly one Toaster instance exists on the Resume Builder page post-fix (no duplicate).

**Also fixed in this pass:**
- **`regenerateLastResponse` data-loss bug** (flagged 12 Aug, never fixed): the AI Assistant deleted the existing AI response from the database *before* attempting to generate its replacement, so any failure during regeneration — a gateway error, a DB write failure — permanently lost a working answer, with the fallback error text existing only in local state (gone on reload, never persisted). Fixed by reordering: generate and confirm-save the replacement first, only then delete the superseded message. A failure now leaves the original answer completely untouched and shows a toast instead of silently losing data — made possible by the Toaster fix above, since this route previously had nowhere to show that toast either.
- **Bookmark save/unsave** used a blocking native `alert()` for signed-out users and swallowed every database error with a bare `console.error` (buttons would silently revert to their prior state, un-explained). Replaced with `toast.error()` calls, consistent with the rest of the app.
- **Auth (login/signup)**: added a password visibility toggle to both password fields (previously no way to check what you'd typed before submitting), `autoComplete` attributes on every field (previously entirely absent — real friction for password managers), unique field `id`s between the two conditionally-rendered forms (both used the literal ids `email`/`password`, harmless today only because React never renders both forms at once), and a loading state + error handling for the "Continue with Google" button (previously: a resolved-but-empty response left the button looking clicked with zero feedback).

**Evidence:** 409/409 tests still passing, TypeScript unchanged at 19, lint clean on every touched file, production build passes. Toaster fix and regenerate fix both re-verified against the real running app, not just read from the diff — screenshots and DOM inspection for the toast, a full send→regenerate→confirm cycle for the chat fix.

**Not attempted in this pass, said plainly:** AI Search and Deep Interview deployment — owner explicitly said to leave both for now ("I think it's not possible, leave it about these two remaining"). CSP's `unsafe-inline` (a full nonce-based rewrite, correctly out of scope — see SEC-02 above). A broader visual redesign of any page — every fix here is a real, verified bug or a concrete missing affordance, not a cosmetic pass; see the accompanying report for the honest updated rating on each surface.

---

### NOTES-01 · Notes workspace built end to end — folders, rich editor, sharing, trash, robot capture — **owner-requested, 16 Aug 2026**

**Found on arrival:** a partially-built Notes feature sat uncommitted in the working tree. The backend was real (notes table, folders table, CRUD routes, an upload route) but three things were built and never connected: **nothing anywhere called `listFolders`/`createFolder`/`renameFolder`/`deleteFolder`**, `note-rich-editor.tsx` (184 lines of tiptap) had **zero importers** — the note body was a plain `<textarea>`, confirmed live by reading `document.activeElement` — and `uploadNoteImage` had no callers. Worse, migration `20260816130000_note_folders_and_attachments.sql` had **never been applied to production**: `note_folders`, `notes.folder_id` and the `note-attachments` bucket did not exist, so the feature would have 500'd on the folders API and failed every image upload the moment it shipped.

**Built, in dependency order:**

1. **Data model** — two new migrations (`20260816140000_notes_workspace`, `20260816150000_note_preview_view`), applied to production and each verified by direct query. `note_folders` gains `color`/`icon`/`parent_id`/`position` (palette constrained by a DB check, not just TypeScript); `notes` gains `deleted_at` so Trash is a soft delete and permanent deletion is never the default; new `note_links` (polymorphic note→note/folder/opportunity/application), `note_shares` + `note_share_recipients`, and `note_attachments`, all with RLS and grants written in the same migration that creates them — the lesson from DB-02, where a table existed for weeks with no GRANT. Two `security_invoker` views (`note_preview_rows`, `note_folder_counts`) so a folder card costs 400 characters instead of a full document, and so the count beside a folder is computed from the same rows the folder actually shows — the APP-16 failure mode (a real number pointing at an empty page) made structurally impossible.
2. **API** — 11 routes. Notes gain views (all/recent/pinned/shared/attachments/archived/trash), sorting, tag and attachment filters, and a shared-with-me path. Folders gain colour, icon, nesting **with cycle detection** (dragging a parent into its own child would otherwise detach both from the tree — they still exist, but nothing renders them, which reads to the user as "my folders were deleted"), and a single bulk-reorder request rather than one PATCH per folder. Plus bulk actions, duplicate, sharing, links, link-targets, attachments, and an upload route that now accepts documents as well as images. Every route checks auth first and carries an explicit `.eq('user_id', …)` alongside RLS.
3. **Editor** — the orphaned editor became the real one and is now mounted in all three places that edit a note. Headings, bold/italic/underline/strike, inline code, code blocks, quotes, highlight, colour, font size, alignment, indent, line spacing, bullet/numbered/task lists, links with auto-detection, tables, resizable and alignable images with alt text, file chips, a drawing pad with four stroke widths, undo/redo — behind a clean toolbar with a More menu rather than 25 buttons at once. **Zero new dependencies**: StarterKit v3 already bundles underline/strike/link/code/undo-redo and `TextStyleKit` covers colour, size and line height — verified against the installed packages' own type definitions rather than assumed, per this repo's AGENTS.md warning that these APIs differ from training data.
4. **UI** — 3D folder cards (CSS perspective and transforms, not a 3D renderer: a browsing grid must stay responsive at a hundred folders) with per-colour gradients, hover tilt and note previews fanning out; the workspace shell with sidebar views, folder tree, drag-to-reorder, grid/list toggle, multi-select with bulk actions and touch long-press, context menus, templates, and per-context empty states; share dialog; link picker; attachments view.
5. **Robot** — kept entirely, including the supplied Spline scene. Its Quick Note now uses the same editor as everything else, the robot is 25% larger (110 → 138), and the composer itself is draggable by its header, clamped inside the viewport, with a resize no longer yanking it back to the robot once the user has placed it.
6. **Opportunity integration** — a Notes section on `/opportunities/[id]` reading and writing the one notes backend, capturing the opportunity and (when the student already tracks it) the application id. Never fabricated: no tracker row means no `application_id` at all.

**Four real bugs found and fixed during the build, each caught by verification rather than review:**
- **A hydration error, live-reproduced**: the note card's date rendered "16 Aug" on the server and "Aug 16" in the browser — `toLocaleDateString` resolves the locale differently across runtimes. Fixed by formatting from a fixed month table in UTC (local `getMonth()`/`getDate()` would still disagree between a UTC server and an IST browser for five and a half hours of every day). Same class as APP-15; the `useClientNow` guard alone did not cover it, because the bug was locale, not the clock.
- **A regex mangled on write** in the upload route's filename sanitiser became `[^@-^_]`, which would have stripped nearly every character from every uploaded filename.
- **The template menu was clipped invisible**: `overflow-hidden` on the New Note pill hid the dropdown entirely. It was in the DOM and focusable but had zero visible presence — found by measuring `getBoundingClientRect()`, not by looking at a screenshot.
- **PostgREST's schema cache** served a stale relationship after the DDL, 500ing `/api/notes` until reloaded — worth knowing for any future migration adding a foreign key.

**Evidence:** **470/470 tests** (up from 430 — 40 new: the folders/bulk/share/links/attachments auth-and-validation boundary, and the sanitiser's XSS boundary plus a round-trip of every tag the editor can emit, since a tag missing from the allowlist is invisible at write time and only shows up as the user's table or colour vanishing on reload). TypeScript unchanged at the 19-error pre-existing baseline (0 new; all 19 remain in vendored Reactive Resume files). Lint clean on every touched file. Production build passes with all 11 API routes, `/notes` and `/notes/shared/[slug]` registered. Live-verified in the browser: the folder browser renders with real colours and counts, the robot measures exactly 138×138, triple-tap opens Quick Note, the composer drags by exactly the distance dragged and stays on screen, the template menu measures 256×565 fully inside the viewport, and the opportunity page's Notes section renders with a working Add Note.

**Also fixed in passing:** `lib/schema-guard.ts` had no entry for `notes`, `note_folders` or the `note-attachments` bucket — the exact drift class OPS-02 exists to catch, and exactly what had just happened. Added those plus the two interview tables; the guard now verifies **31 objects** and was re-run green against production. The guard's own "every check fails" test had also silently stopped covering `hub_messages`; that is now honest again.

**Not done, said plainly:** the shared-note page is view-only by design (anonymous editing would be an unauthenticated write endpoint with no identity behind it — editing is offered per named recipient instead, enforced by the `notes_update_shared_with_me` policy). Folder drag-to-reorder is implemented in the sidebar only, not on the 3D cards. Live QA covered the workspace, robot, template menu and opportunity section; the share, trash and mobile flows are wired and typed but not yet exercised live.

---

### AUDIT-16AUG-FULLPASS · Complete live walkthrough, landing page through logout — **owner-requested, same day**

Full detail with evidence for every item below is in [`AUDIT-2026-08-16.md` §10](./AUDIT-2026-08-16.md#10-third-pass-same-day-full-live-walkthrough-landing-page-through-logout). Signed into the owner's own real account via a magic link (their own explicit ask, distinct from this file's usual "purpose-created test account" convention). Every authenticated and unauthenticated surface was exercised live; five real bugs surfaced, all fixed and re-verified live after the fix.

#### UI-04 · No navigation in the 768–1023px viewport gap; 5 of 12 destinations unreachable below 768px — **High**
`app/(protected)/layout.tsx` composed `DashboardSidebar` (`hidden lg:flex`, ≥1024px) with `MobileBottomNav`/`DashboardHeader` (both `md:hidden`, <768px) — leaving 768–1023px (tablets, unmaximized laptops, split-screen windows) with zero nav chrome at all, confirmed via a screenshot at 900px showing bare page content and nothing else. Below 768px, the 6-item bottom tab bar and 2-item header left Settings, Support, Certifications, Mock Interview, and **Sign Out** unreachable through any in-app navigation on a phone. **Fixed:** aligned the sidebar to `md:flex` (closes the gap exactly where mobile chrome disappears) and added a hamburger menu to the mobile header for the five items the bottom tab bar has no room for, Sign Out included, wired to the same `logoutAction` the desktop sidebar already used. Re-verified at 900px (full sidebar, all 12 destinations) and 375px (hamburger menu opens, all five items present, closes on backdrop click and on real navigation) on the dev server.

#### APP-13 · Sending a Hub message could crash the whole page — **Critical**
A live Supabase Realtime INSERT payload for `hub_messages` came through incomplete (caught directly via the fix's own diagnostic log as a literal empty object `{}`, reliably reproduced twice sending 5 messages in quick succession). The cascade: `resolveSender(undefined)` → `encodeURIComponent(undefined)` silently becomes the string `"undefined"` → `/api/hub/senders?ids=undefined` queries Postgres with an invalid UUID literal and 500s → separately, the malformed message (missing `created_at`) still got added to state, and `hub-client.tsx`'s clear-chat filter called `.replace()` on it directly with no guard, crashing the page to the global error boundary. **Fixed at every layer**: `resolveSender` short-circuits on a falsy id without firing a request; the realtime handler drops any payload missing `id`/`sender_id`/`created_at` before it reaches state; `/api/hub/senders` validates real UUIDs instead of "non-empty after trim," so a bad value now resolves to `{senders: {}}` instead of 500ing; the clear-chat filter defaults a dateless message to visible instead of crashing. Live re-test: 5 rapid messages sent via the real `sendMessage` flow — zero crashes, console confirmed the guard caught the same malformed-payload condition twice. Edit-after-send and delete (pre-existing features) were exercised in the same session and needed no changes.

#### APP-14 · Bookmark existence check 406'd on every unbookmarked card — **Moderate**
`useBookmark` used `.single()` for "is this saved yet," which 406s on the ordinary zero-row case. Confirmed live: `/search` (14+ cards) produced 14+ real console 406s in a completely fresh tab. The feature itself worked correctly either way (the code already checked `if (data)`) — this was pure console-error spam that could mask a real error. **Fixed:** `.single()` → `.maybeSingle()`.

#### APP-15 · Real React hydration error on the Tracker — **Moderate**
`daysBetween(from, to = new Date())` computed "now" via a bare default parameter, evaluated separately by the server during SSR and the client during hydration — a deadline close to a day boundary could render different badge text ("3d left" vs "2d left") between the two, throwing a genuine `Minified React error #418`, reproduced in an isolated fresh tab. **Fixed:** `to`/`now` is no longer defaulted internally; a new `useClientNow()` hook (same pattern as `hub-message.tsx`'s timestamp fix) returns `null` until a post-hydration effect sets the real value, so the server render and the client's first render agree exactly (no badge yet) before the real one fills in a frame later.

#### APP-16 · Dashboard "10 saved" count led to an empty page — **Moderate**
Dashboard's Progress Summary counted every row in `bookmarks` (`SELECT count(*)`); the page it links to (`/profile/saved`) correctly filters out bookmarks whose opportunity has passed its deadline or closed before rendering. A fully-stale bookmark list (all 10, in this case) produced a real, "waiting for action"-framed number pointing at an empty page — the same class of problem as this file's own APP-11. **Fixed:** the dashboard's count now applies the identical filter `/profile/saved` uses, computed from the same joined data. Re-verified live: both surfaces now agree exactly (0), and the "Review Saved Opportunities" action card correctly stopped appearing.

**Re-confirmed working, not a new finding:** the certifications stale-cache issue flagged mid-session (page showing 15,765 instead of the live ~20,753) was `unstable_cache` serving a pre-ingestion snapshot, fixed earlier the same session by bumping the cache key versions (commit `e46e919`) and confirmed live at 20,720 with all 18 providers present.

**Evidence:** 409/409 tests, typecheck clean on all 8 touched files (19-error pre-existing baseline elsewhere unchanged, confirmed via `git diff`), lint clean (two legitimate `react-hooks/set-state-in-effect` cases documented with the same disable-comment convention `hub-message.tsx` already established), production build passes. Every bug above was reproduced live before being logged and re-verified live after the fix.

---

## ⬜ Open — Critical

*(none)*

---

## ⬜ Open — High

*(none)*

---

## ⬜ Open — Moderate

| ID | Issue | Where |
|---|---|---|
| DATA-06 | 105 listings (2.5%) have no description — SmartRecruiters 95, Lever 10. Re-checked 12 Aug: **106**, essentially unchanged | `opportunities` |
| DATA-07 | `trust_tier` is `3` on all 3,989 live rows — re-checked 12 Aug via per-value count query (a prior row-fetch sample undercounted due to PostgREST's 1000-row cap, same class of bug as APP-08's certifications finding) — **still flat, the trust engine still does not discriminate.** `verified=true` did improve: 5 → **214** | `opportunities` |
| CODE-01 | TypeScript errors shipped unchecked behind `typescript.ignoreBuildErrors: true` — **19**, re-checked 16 Aug (was 36; dropped as a side effect of the CODE-02 test-infra fix surfacing type errors in files `vitest` had silently never been running) | `next.config.ts` |
| CODE-02 | 6 test files call Lingui's `t`/`msg` macro tags, which need a Babel/`babel-plugin-macros` compile step Vite has no equivalent of — fixing needs adding `@vitejs/plugin-react`, which hit a real peer-dependency conflict (React 19 vs. this project's Vite/Vitest version) on 16 Aug. 1 of the original 7 affected files (`base.test.tsx`) was fixed outright — 4 real tests recovered | `libs/resume/section*.test.tsx`, `features/command-palette/pages/preferences/*.test.tsx`, `features/resume/dialogs/resume/template/gallery.test.tsx` |
| APP-12 | Resume Optimiser scoring inconsistency: same content scored 70 → 68 → 57 across baseline/Resume A/Resume B, evaluated independently even at `temperature: 0.0` — found and documented 16 Aug, not fixed (needs a per-requirement diff of the three evaluations, not a speculative patch) | `features/resume-toolkit/services/ai/ats-v2-intelligence.ts` |

---

### DATA-04 · Duplicate rows — **closed, 17 Aug 2026: no duplicates remain**
Re-verified against the full catalogue rather than a sample (the 12 Aug check
looked at 1,000 rows and was correctly judged too small to clear the finding):
**0** groups share an `apply_url`, and exactly **1** group shares title+company
— two Amazon postings both titled "Data Center Security Specialist, APJC Asset
Team". Those are not duplicates: their `apply_url`s carry different Amazon job
ids (`10432673` and `10432672`), so they are two genuinely separate openings
that happen to share a title. Nothing to dedupe.

---

### DATA-05 · Past-deadline rows still `Published` — **data corrected 17 Aug 2026, cause still open**
The count has moved 144 → 6 (12 Aug) → 25 → **177** (17 Aug). It is growing,
so the reconciliation is not keeping up — this is not a one-off backlog.
All 177 were expired directly (`status='Published' and deadline < now()` →
`Expired`), verified at **0** immediately afterwards against 3,649 published
rows.

**The cause is not fixed.** Correcting the rows is the same treatment applied
on 10 and 16 Aug, and it has now recurred three times, which is itself the
evidence that the scheduled reconciliation either is not running in production
or does not cover every provider. That needs a look at the cron's actual run
history, not another data patch — logged below as the remaining half of this
issue.

---

## ⬜ Open — Low

| ID | Issue |
|---|---|
| CLEAN-01 | ~500 MB of build artifacts, logs and debug screenshots committed (audit §7.1) |
| CLEAN-02 | 19 superseded documents (audit §7.3) |
| CLEAN-03 | ~30 verified-dead code modules (audit §7.4) |
| CLEAN-04 | 4 `opportunities_backup*` tables holding 5,158 rows total |

*(SEC-03, CODE-03 fixed 16 Aug — see AUDIT-16AUG-SEC above. CLEAN-05 confirmed already resolved — no `/resume/test-engine` or `/test-ats` routes exist; likely closed by the pre-16-Aug `chore(resume-toolkit): remove leftover dev-only pages` commit.)*

---

## ⏸️ Deferred

### LEGAL-01 · Reactive Resume licensing — **needs your decision, not a code change**
`features/resume/` and 197 other files import `@reactive-resume/*`, vendored into `frontend/packages/`. The fork at `/Users/laxmansirvi/reactive-resume` is relabelled `"resumeai"` / MIT, but upstream Reactive Resume is **AGPL-3.0** from v4. Relabelling does not change the licence, and AGPL requires offering source to users of a network-deployed service.
**Action:** confirm which upstream version and licence the fork came from before any public launch.

### OPS-01 · Queue consumer not running in production
`ingestion_queue` holds **186 pending** items. The consumer only runs via `npm run cron:consume`. Deliberate for now — the direct provider path is carrying ingestion successfully (98.7% freshness).

---

## Feature completion

| Feature | 10 Aug | 12 Aug | **16 Aug (today)** | Note |
|---|---|---|---|---|
| Hub | 0% | 90% | 90% | Verified live again this pass (sender names, avatars, images all correct) — not otherwise touched |
| Search | 85% | 85% | **88%** | DATA-08 fixed live (138 self-contradictory Onsite/Hybrid+Remote listings) |
| Tracker | 90% | 90% | 90% | Not touched |
| Resume: builder | 80% | 85% | 85% | Verified live — full section coverage, real autosave, correct schema validation. Not otherwise touched |
| Resume: extract | 75% | 75% | 75% | Not touched |
| Resume: ATS | 85% | 98% | 98% | Re-verified live end to end again this pass — no change, still solid |
| Resume: optimisation | 35% | 99% | 99% | Full live run re-verified (baseline→A→checklist→B→2 real PDFs downloaded and byte-checked). One real, undiagnosed scoring-consistency finding logged (APP-12) but not fixed |
| AI Search | 35% | 50% | 50% | State corrected, not improved — real deployment tooling now exists in the agent repo but has never been run; still `AI_AGENT_URL=http://localhost:4300`, confirmed unreachable |
| Certifications | 30% | 90% | **92%** | Found and fixed a real bug (APP-09): a transient query timeout was being cached as a fake "0 certifications" for 15 minutes, indistinguishable from an empty catalogue |
| AI Voice Interview | 2% | 2% | **~35–40%** | The 2% figure was stale — two commits since 12 Aug built a real, 1,586-line Opportunity Radar-side integration (correct auth, RLS, a genuine LiveKit voice room, honest report rendering). Still 0% *deployed*: no env vars set anywhere, DB migration likely unapplied to production, both blocking the feature from working even in demo form |
| Auth | 90% | 90% | **92%** | 2 real bugs fixed (AUTH-01, AUTH-02) — signup showing the wrong form, and every new signup's display name silently becoming their email prefix |
| Profile / Settings / Notifications | 85% | 90% | 90% | Not touched |
| AI Assistant | 75% | 80% | **83%** | 2 real bugs fixed: a hydration mismatch on the send button (APP-10), and a live fabrication instance — claiming to show opportunities while displaying none (APP-11) |
| **Overall** | **~58%** | **~80%** | **~82%** | Driven by real bugs found and fixed via live user-experience testing, not new feature work — AI Search and Voice Interview remain the two biggest levers, both blocked on deployment/ops rather than code |

---

## Next up

1. **Deploy the AI Search agent and the DeepInterview voice agent** — both remain the single biggest lever on the overall completion number. Neither needs new code; both need real ops work (VM/hosting, API keys, one end-to-end proof run). See the migration/handoff guidance delivered alongside the 16 Aug audit for concrete next steps and ready-to-use prompts for whichever agent picks up the deployment work.
2. **Apply migration `20260816090000_fix_new_user_name_metadata_key.sql`** to production — the app-layer fix (AUTH-02) already covers new signups, but the trigger itself is still wrong for any signup path that doesn't go through this form.
3. **Diagnose APP-12** (the 70→68→57 scoring swing) properly — needs a side-by-side diff of which specific JD requirement flipped `satisfaction` between the Resume A and Resume B evidence evaluations, not a speculative fix.
4. **Decide the recovery UX for AI Assistant's `regenerateLastResponse` bug** (flagged 12 Aug, not fixed) — it can delete a working answer before confirming the replacement saved. Needs an owner call between "keep the old message until the new one lands" vs. "show a distinct failure state," not something code review alone should decide.
5. **Check the first scheduled `link-sweep` run** (`0 2 * * *`) once it lands — the 12-row proof-run confirmed correctness, not full-catalogue throughput within the 300s ceiling.
6. **Re-verify DATA-04** (duplicate rows) properly — 12 Aug's quick sample was too small to confirm or clear it.
7. Everything else in **Open — Moderate** and **Open — Low** below — no Critical or High issue remains open anywhere in the project.

---

## Log

| Date | Change |
|---|---|
| 10 Aug 2026 | Full audit; 25 issues catalogued |
| 10 Aug 2026 | DB-01 – DB-05 fixed and verified; database drift resolved |
| 10 Aug 2026 | OPS-02 fixed — schema guard added; verified green against production (24/24 objects) |
| 10 Aug 2026 | APP-01 fixed — copilot mockup unlinked and replaced with an honest placeholder; dead `workspace-tools-panel.tsx` deleted. **No critical issues remain open.** |
| 10 Aug 2026 | APP-02 built — Resume Optimisation generation wired, ATS-safe PDF export, full UI replacing the APP-01 placeholder. 277/277 tests, TypeScript unchanged at 36, build passes, UI screenshot-verified via a temporary unprotected route (then deleted). Not yet run end-to-end against a real session. |
| 10 Aug 2026 | Committed `f28e6c4` and deployed to production from `restore-june19` (the production branch). Deployment `opportunity-radar-h5741titk` Ready. Post-deploy check: `/` and `/login` return 200; `/search`, `/tracker`, `/resume`, `/certifications`, `/ai-search` all 307 to `/login?next=…` with the destination preserved. |
| 10 Aug 2026 | Committed `3aa4612` (APP-02) and pushed to `restore-june19`. Post-push spot check only: `/resume/copilot` → 307, `/api/cron/health` → 401, matching pre-existing behaviour — **not** a confirmation the new feature works live, since that needs a real session. |
| 10 Aug 2026 | SEC-01 fixed and pushed (commit `86eef58`) — default rate limit (30/hour) closes the unlimited-feature gap for every `AIFeature` without a tuned entry. 279/279 tests. |
| 10 Aug 2026 | DATA-01 re-investigated with the owner present: only 170 of the audit's 376 rows were real dead-ends (Stripe 126, HighRadius 44) — the other 206 (Databricks, MongoDB, Fivetran) were a false positive from URL-pattern matching; verified by clicking through live samples per company. No working link exists for the 170, including the audit's suggested `job-boards.greenhouse.io` fallback. Expired all 170 in production; verified via real row-select before and after. |
| 10 Aug 2026 | DATA-02 re-verified and fixed: 22 of 32 published Internshala rows genuinely show the closed banner (curl + browser spot-check), matching the audit's 27-total once the 5 already-expired are added back. Expired the 22 in production. Taught `InternshalaProvider` the closed-page marker at both call sites so it stops re-confirming closed postings as live. 285/285 tests, TypeScript unchanged at 36. |
| 10 Aug 2026 | DATA-03 built and shipped: `lib/ingestion/link-checker.ts` + `/api/cron/link-sweep`, scheduled nightly at `0 2 * * *`. Scope deliberately narrower than "detect any dead link" — only DNS/connect failure, 404 and 410 auto-expire; everything else is recorded, not acted on, per the DATA-01 lesson that status codes alone can't tell a real page from a broken one. 294/294 tests. Live proof-run against production (12 rows, real service-role client) verified with a real row-select, not just mocks. **No High or Critical issues remain open.** |
| 10 Aug 2026 | Resume Optimiser UI redesigned, owner-requested ("not consuming high white space... more professional, more interactive"). Results page: score rendered as an SVG ring in one unified header card instead of two stacked cards; Resume A/Resume B moved from full-width stacked cards to a side-by-side grid with inline scores and download buttons; suggestion checklist tightened (compact rows, progress fraction + bar) and made genuinely interactive (click to toggle, disabled while pending). Intake form: resume-source toggle changed from two loose buttons to a single segmented control, upload dropzone and "Past runs" list tightened. Found and worked around UI-03 (Tailwind theme-collision bug, logged above) during this pass. Screenshot-verified both pages via temporary unprotected routes (`dev-preview-optimizer`, `dev-preview-optimizer-form`), both deleted after. TypeScript unchanged at 36, lint clean on touched files. |
| 10 Aug 2026 | APP-04 fixed: full audit + rewrite of the ATS scoring pipeline, owner-requested ("evidence based ATS score... not word matching... trace and correct the complete underlying pipeline"). Deleted the entire legacy keyword-matching engine that was running alongside the real V2 engine on every request and silently getting persisted to the database instead of the score shown on screen; applied the APP-02 Builder-shape-resume fix (previously deferred) to the ATS Checker; made the job description optional with an explicit `resume_only`/`targeted` mode; replaced the generic AI-failure banner with the real failure stage and message; unified the gap→checklist logic between the ATS Checker and the Optimiser into one shared module; moved the CGPA rule out of the AI-coaching path into a pure, directly-tested function; hardened both V2 AI prompts (JD-extraction breadth, evidence-matrix anti-templating) based on failures caught live during verification — including a live-caught JD-extraction regression (1 requirement instead of 19) fixed on the spot by strengthening the extraction validator. Live-proven against real providers: weak vs. strong resume on the same JD scored 34 vs. 70 (evidence-based discrimination, not keyword overlap); 16 gap-reason texts came back with 16 distinct openings (the templating bug from the report, confirmed fixed); a malformed JD cleanly failed across all 6 configured providers with no fabricated score. Added a missing `maxDuration` (this route had none, same class of bug as APP-02's fix on `/api/resume/optimization`). 342/342 tests (2 legacy-only files deleted, 5 new), TypeScript unchanged at 36, lint improved (24→9 errors on touched files from deleting legacy code), production build passes. **No High or Critical issues remain open.** |
| 11 Aug 2026 | APP-05 built: Hub (global community chat) taken from uncommitted WIP to a working, owner-requested "WhatsApp-style group chat" with maximum reasonable feature scope — text, images with captions, replies, editing, delete-own, per-user clear-chat, profile viewing, and a live presence ("N online") indicator, plus a full visual redesign. Six real bugs found and fixed along the way: a dead `/auth/login` redirect, a missing RLS UPDATE policy that would have silently blocked all edits, a second occurrence of the UI-03 Tailwind `max-w-md` collision (squished the profile modal to 16px), an unhandled-rejection bug that left the profile modal stuck loading forever on any fetch failure, a hover-only action menu that was completely unreachable on touch devices, a real SSR/client hydration mismatch on every message timestamp (`toLocaleTimeString` depends on timezone), and a collapsed-to-nothing image bubble caused by `next/image fill` combined with a percentage width on an intrinsically-sized flex ancestor. New migration (`hub_images_and_edit`) applied directly to the linked production database and verified live via direct query (columns, policy, and the new `hub-attachments` bucket all confirmed present). Every interactive path verified via a temporary preview route using direct DOM/computed-style inspection, not just screenshots — several of the bugs above were only conclusively caught that way. 342/342 tests (2 `schema-guard` fixtures updated for the new bucket), TypeScript unchanged at 36, lint clean, production build passes with all 4 new Hub API routes present. |
| 12 Aug 2026 | Strict, live-verified audit pass across 6 features (see AUDIT-12AUG above): Certifications (2 bugs, verified against all 20,634 rows), AI Assistant (removed a live unauthenticated service-role endpoint, fixed a masked type error, flagged one unfixed data-loss bug for an owner decision), AI Voice Interview (a real DB race condition caught before its migration ever ran in production, a frozen-UI network bug), AI Search (clean, 0 bugs). UI-03 finally fixed at the root instead of worked around a 5th time — the full regression sweep the prior 4 attempts kept deferring, 234 call sites mechanically rewritten across 15 files. |
| 12 Aug 2026 | ATS Score and Resume Optimiser given their first genuinely live, real-session verification — the one gap every prior pass on either feature explicitly flagged as needing the owner's own login, closed via the owner's own sign-in. 2 bugs found and fixed (a resume-picker UUID display bug in both features, a responsive-design gap on their shared `/resume` entry point) plus one UX-clarity fix. Both pipelines confirmed genuinely working end to end: real evidence-based scores, real Resume A generation with an honest re-score, a real downloaded PDF verified byte-for-byte. |
| 12 Aug 2026 | Multi-key-per-provider rotation built for AI Assistant/ATS/Optimiser (owner-requested, demo-day resilience): Gemini, Groq, OpenRouter and Ollama each rotate through several owner-supplied keys before falling to a weaker provider. Found and fixed a real bug while wiring it up — `gemini-2.5-flash` (pinned) 404s on newer Google Cloud projects; switched to `gemini-flash-latest`. Mistral added as a new provider at the owner's request, after flagging its free tier's data-training tradeoff. Live-verified against real traffic, not mocks: one real call failed on 3 keys and succeeded on the 4th, with the caller getting a clean response regardless. 14 new tests. 359/359 total, TypeScript unchanged, build clean. Committed `f9d08f0` and `b271881`, both pushed to `restore-june19`. Keys added to Vercel production and redeployed by the owner. |
| 12 Aug 2026 | Feature-completion table corrected: Certifications was showing a stale 45% ("no ingest pipeline yet") despite APP-08 (11 Aug, logged above) already having populated 20,634+ real certifications — corrected to ~90%. ATS/Optimiser bumped to 98%/99% now that the owner-login caveat is closed. AI Assistant added to the table for the first time. Live-rechecked the tracker's remaining open DATA items: DATA-05 improved sharply (144→6 stale-but-published rows); DATA-06 and DATA-07 unchanged (trust_tier still flat 3 on all rows, though `verified=true` improved 5→214); DATA-04 not re-verified (sample too small). Schema guard re-run against production: **all 22 tables, 5 buckets, 2 RPCs still reachable** — zero drift since 10 Aug. Overall completion: ~75% → ~80%. |

| 16 Aug 2026 | Full live user-experience audit (see AUDIT-16AUG above) via a fresh, purpose-created test account (not one of the five real `auth.users` accounts). 9 real bugs found and fixed, all re-verified live after the fix: `/signup` rendering the Login form (AUTH-01), every new signup's display name silently becoming their email prefix (AUTH-02), UI-01/UI-02 finally fixed instead of just tracked, 138 self-contradictory Onsite/Hybrid+Remote listings from a real Unstop ingestion bug (DATA-08), a transient DB timeout on Certifications being cached as a fake "0 results" for 15 minutes (APP-09), a real hydration mismatch on the AI Assistant (APP-10), the AI Assistant fabricating "here are some opportunities" with none attached (APP-11), and zero rate limiting on voice-interview session creation (SEC-04). One real scoring-consistency finding logged but not fixed (APP-12, needs deeper diagnosis). 405/405 tests (up from 359 — resume-toolkit work between sessions added its own), TypeScript unchanged at 35, lint clean on all touched files, production build passes. New migration `20260816090000_fix_new_user_name_metadata_key.sql` written, not yet applied to production. |
| 16 Aug 2026 | Independent code-level audits of AI Search and AI Voice Interview corrected two stale numbers. AI Search: real Oracle Cloud deployment tooling now exists in the agent repo but was never run — `AI_AGENT_URL` still points at an unreachable localhost address; state held at 50%, not improved, but the actual next step (prove the pipeline locally before VM work) is now concrete. AI Voice Interview: the stale "2%, nothing built" was wrong — two commits since 12 Aug built a real 1,586-line integration (LiveKit voice room, correct auth/RLS, honest report rendering) — corrected to ~35–40%, but zero env vars are configured anywhere and the DB migration is likely unapplied to production, so the feature still cannot function even in demo form. Overall completion: ~80% → ~82%. |
| 16 Aug 2026 | Security cleanup pass (see AUDIT-16AUG-SEC above): SEC-05 (auth-after-validation ordering) fixed; SEC-02 (CSP `unsafe-eval`) tightened for production, verified against a real production build with zero CSP violations across the heaviest surfaces (Resume Builder incl. live PDF generation, Hub, AI Assistant); SEC-03 and CODE-03 (dead code with a real open-redirect landmine, and a route that always 500s) deleted; CODE-02 partially fixed (2 of 7 broken test files now genuinely pass, the rest excluded with a precise documented reason instead of left silently red). TypeScript errors dropped 35→19 as a side effect. 409/409 tests, build clean. |
| 16 Aug 2026 | Design/UX polish pass across Auth, AI Assistant, Search, Hub, Certifications (see AUDIT-16AUG-POLISH above), owner-requested to push every surface's rating up. **Found the highest-value bug of the whole audit in the process**: sonner's `<Toaster/>` was mounted on exactly one page in the entire app (the Resume Builder), so every `toast()` call anywhere else — ATS Checker, Optimiser, AI Search, Tracker, notifications, bookmarking — silently rendered nothing; confirmed live by watching a real ATS check's loading toast fire in code and produce zero visible feedback. Fixed by mounting one global Toaster in `app/layout.tsx`. Also fixed: the `regenerateLastResponse` data-loss bug flagged since 12 Aug (never actually fixed until now — reordered to generate-then-delete instead of delete-then-generate), bookmark error handling (was a blocking `alert()` + silent console-only failures, now toasts), and real auth-page affordances (password visibility toggle, `autoComplete` on every field, unique form-field ids, OAuth loading/error state). 409/409 tests, TypeScript unchanged at 19, build clean. |
| 16 Aug 2026 | From-scratch landing page (owner-built separately) integrated into the main app: `app/page.tsx` rewritten to compose it (kinetic-grid hero, interactive globe, two GSAP/Framer scroll scenes, FAQ scroller, footer), 6 components + 7 images ported, CTAs wired to real `/login`/`/signup` routes, one real mobile layout bug found and fixed (Scene Two's headline overlapping content on small screens). Deployed to Vercel production. |
| 16 Aug 2026 | Cross-feature ATS score-consistency fix, owner-reported live ("resume optimisation is giving more score comparatively ATS score" for the identical resume + job description) — the same root cause as APP-12 (independent AI evidence evaluations aren't perfectly reproducible even at `temperature: 0.0`) but for the ATS Checker vs. Optimiser baseline specifically, not the within-run baseline→A→B drift APP-12 tracks. Fixed by having both features reuse a matching structuredJd/evidenceMatrix pair — computed once by whichever feature ran first for that exact resume + JD — instead of each independently re-asking the AI (`lib/resume-optimizer/shared-evaluation.ts`); the score is then deterministically recomputed from the shared inputs, so it's provably identical, not just less likely to differ. **Live-verified end to end**: the same saved resume + a fresh job description scored 60 via the ATS Checker, then 60 via the Optimiser — matched exactly, confirmed via network inspection that the Optimiser's request completed correctly rather than accidentally reusing UI state. Also shipped in the same pass: per-row delete + a 6-item cap on both ATS Score History and AI Optimiser History (owner-requested), a working PDF download for saved resumes and for the ATS/Optimiser baseline resume (previously only the Optimiser's generated polished/target resumes had one), and Preview + Download surfaced directly on the `/resume` home card's latest-result summary. 409/409 tests, TypeScript clean on touched files, production build passes. |
| 16 Aug 2026 | Certifications data-integrity fix, owner-reported: Coursera courses hosted by a partner account named "Google"/"Microsoft"/"Amazon" were displayed and redirect-attributed as if issued directly by that brand, and every Coursera row was unconditionally marked free when in practice only the audit is free — the certificate is a paid add-on on nearly every course. Root-caused to `fetchCoursera()` setting `provider` straight from the Coursera partner API's own `name` field and hardcoding `is_free: true` with no per-course pricing check. Fixed ingestion (provider is now always "Coursera", with "In collaboration with X." folded into the description since a dedicated column isn't achievable without direct Postgres DDL access, which this environment doesn't have) and corrected all 19,256 existing Coursera rows in production directly via the service-role key — live-verified (0 mislabeled, 0 wrongly-free after the run; a spot-checked "Google Data Analytics Capstone" course now shows provider "Coursera", price "Paid", and the collaboration note in its description). Added 3 new providers the owner named — Cisco Networking Academy (18, genuinely free, direct-issued), Udemy (10) and DataCamp (8) — both confirmed live to sit fully behind a Cloudflare bot challenge (even robots.txt itself), so their catalogues are curated static lists in the same pattern already used for freeCodeCamp/W3Schools rather than live-scraped or fabricated. Raised the Coursera/edX/Simplilearn ingestion caps; catalogue grew 20,634 → 21,153 real, correctly-labeled certifications (short of the owner's 25–30k target — the real combined catalogue size across all 10 providers tops out there once dead/duplicate/non-English courses are filtered, not a capacity limit). 409/409 tests, build clean. |
| 16 Aug 2026 | Hub (Global Chat) audited against 3 owner-requested features: message editing after send and photo sending were already fully built (editing has no time limit and shows an "edited" label live to other users; photo upload already supports captions) — no changes needed. Emoji support did not exist at all (confirmed via full search — no picker, no reactions, no library) — added a categorized emoji popover to the message composer that inserts at the cursor position, no new dependency. |
| 16 Aug 2026 | Privacy Policy and Terms of Service rewritten from ~5 thin sections each to detailed, accurate documents matching what the app actually does — including an honest "AI Processing & Model Providers" section naming the real providers in use (Google Gemini, Groq, Mistral, OpenRouter, LiveKit for voice interviews), Hub content-visibility disclosure, data retention, and a sticky in-page table of contents on both pages (the grid layout already reserved space for one; it was just never built). Support page rebuilt from 4 static cards into a real FAQ center — 7 grouped, expandable question sets covering every feature, addressing (among others) the Coursera-labeling and free-vs-paid-certificate questions the owner raised directly. Settings gets a Member Since date, a Name field linking to the real profile editor (previously not cross-linked from Settings at all), Help & Legal cross-links, and a genuine bug fix: the notification toggles caught save errors but only `console.error`'d them — the exact class of silently-broken feedback fixed elsewhere in this project — now shows a toast either way. |

> The schema fixes applied directly to the Supabase database, so DB-01 – DB-05
> were live in production from the moment they were run. This deploy shipped the
> migration files, the type additions and the three documents.
